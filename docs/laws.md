# Laws — catalog

Two-level scheme + full law index. Lookup by `P<n>` or `L<n>`.

- `P<n>` = ARCHITECTURAL PRINCIPLE. Prose source `ARCHITECTURE.md`. **Its old wording "law N" means `P<N>`** — same numbers, renamed so `L` is the harness alone.
- `L<n>` = EXECUTABLE HARNESS LAW. `tools/RailCheck/L<n>_<Name>.cs`, or an inline method in `Program.cs` (marked *inline*). Run: `cd tools/RailCheck && dotnet run -c Debug` — exit 0 green, 1 red.
- `L-A`…`L-F` = boundary law, `docs/boundary-law.md`, letters, ride/refuse rules. Untouched here.
- NOT interchangeable: harness `L11` (no `LocalizedTextBind` rides covered) ≠ principle `P11` (UiEventMap repaint).
- Origin `incident` = header cites an observed failure (dated session / quoted report / log line / measured number / named breaking commit).
- Origin `principle` = reasoning, decompile, developer mandate or audit; no observed failure. A generic "this repo has paid for this class" is NOT incident evidence.
- Origin `unclear` = header does not say. Listed in Attention.
- Guard = literal tokens in the law's file: `premise-changed`, `POSITIVE CONTROL`, both, neither. See Attention — `neither` ≠ "checks nothing".

## Principles

- **P1 — TWO PRIMITIVES, AND NOTHING IS SILENT.** Intent (client→host gesture) + Delta (host→peers state) are the only two things on the wire; join/reconnect = native save transfer, not a delta; what a frame cannot carry is NAMED, never silently skipped.
  - pending amendment: 0xB6/0xB7 = third category, ephemeral presentation delta — `docs/design-event-windows.md` §4
  - laws: L34 L37 L39 L40 L41 L61 L62 L70 L71 L83 L93 L106 L107 L120 L122 L124 L125 L127 L128 L134 L135 L136 L142 L143 L321
- **P2 — ONE PLACE NAMES THINGS.** `IdentityResolver` only; stable game ids; path grammar `root.Member.Member#elemKey`; resolved symmetrically on the client over its own graph; no stable key → subtree EXCLUDED + reported; never index-addressed.
  - laws: L10 L14 L17 L22 L28 L56 L58 L62 L101 L106 L107 L112 L113 L140
- **P3 — THE IDENTITY BOUNDARY.** Rail projects VALUES. Create/destroy/re-identify = hand-written structural layer, the ONLY bespoke sync code allowed. Authoritative logic behind a native setter (reward chains, campaign writes, `Complete()`) is the host's. A projector may not write what a simulation mints.
  - laws: L2 L3 L11 L29 L30 L31 L35 L36 L49 L66 L67 L69 L77 L81 L96 L110 L121 L126 L128 L131 L137 L147 L320
- **P4a — BLOCK-FIRST INTENT SEAMS.** On a client outside `SyncApplyScope` a native state mutation is BLOCKED and converted to an intent; the HOST runs the native method; result mirrors back on the Delta rail. Presentation staging may proceed, model writes may not. One gate: `IntentRail.ShouldRunNative`.
  - laws: L12 L19 L24 L26 L32 L36 L42 L63 L65 L69 L76b L99 L110 L111 L138 L320
- **P4b — SIM GATING.** Client clock not frozen → host-only sim funnels prefix-skipped on the client (`LevelHourlyUpdateCrt`, `Research.Update`), reschedule preserved.
  - laws: L42 L63
- **P4c — PRESENTATION SEAMS ALTER NOTHING.** Replay what the model already says. Never block, never write, never throw into game code.
  - laws: L158 (written 2026-08-07, the same day the hole was ruled real — see Attention). Not "jointly defended with P11": P11 laws assert a repaint is REACHED, which a seam that also writes model state passes.
- **P5 — TACTICAL SCOPE IS SHARED AND CONCURRENT.** Six peers command six soldiers at once. First-to-act-wins, no ownership table, no ledger a reload can lose. Which peer plays a mission = tactical scope, not a permission.
  - laws: L27 L44 L45 L47 L65 L68 L75 L76a L78 L80 L97 L99 L104 L111 L123 L130 L132 L139 L140 L141 L144 L145 L146 L149 L165 L85 L320 L321
- **P6 — DETERMINISM / CANONICALITY.** Sorted roots/children/subkeys, fixed metadata field order, byte-identical deltas on both peers. Serializer blobs licensed ONLY as structural payloads.
  - laws: L2 L4 L5 L6 L7 L10 L13 L15 L16 L20 L34 L35 L43 L66 L73 L76a L103 L126 L128
- **P7 — CONVERGENCE.** Idempotence under redelivery; seq-gap → resync; CRC per path-subtree = drift backstop; reject → scoped `ForceReemit` + the family's reconverge. Never log-only.
  - laws: L1 L7 L8 L12 L13 L25 L27 L41 L44 L57 L58 L98 L100 L105 L123 L131 L133 L137 L146 L150 L152 L153
- **P8 — APPLY SCOPE.** Every client apply wraps itself in `SyncApplyScope`: an apply never echoes back as an intent; apply-driven repaints stay suppressed at intent seams + the storage gates.
  - laws: L21 L51 L65
- **P9 — THE JOURNAL IS OBSERVATIONAL.** Written post-pipeline, never in the decision path; debug builds OK.
  - laws: none
- **P10 — PARITY IS BLOCKING.** Mod set, DLC set, per-mod settings, the GAME build and OUR build must match on every peer — the save-graph shape has to. An unresolvable def "cannot happen" → LOUD, never silent.
  - laws: L108 L114
- **P11 — REPAINT IS THE RAIL'S OTHER HALF.** A mirrored value reaches the screen through the game's OWN read-direction refresh (`UiEventMap`, `UiNativeRepaint`) — never a hand-rolled paint, never a lifecycle transition where the model can express it.
  - laws: L18 L21 L26 L38 L45 L46 L47 L51 L52 L54 L60 L77 L79 L81 L83 L92 L93 L95 L96 L98 L101 L102 L104 L115 L116 L117 L118 L124 L127 L129 L130 L135 L138 L139 L141 L142 L144 L147 L148 L149 L151 L156 L163 L164 L165 L86
- **P12 — UNIVERSAL-FIRST.** Unnumbered in `ARCHITECTURE.md`, asserted throughout. ONE generic mechanism per layer; the per-subsystem copy is the "macaroni factory" the mandate forbids. One value rail, one intent engine, one nonce allocator, one dedup, one repaint primitive.
  - laws: L32 L50 L99 L143 L155
- **P13 — NO QUORUMS, NOBODY KICKED. PROPOSED — not in `ARCHITECTURE.md`.** Developer mandate, stated only in the corpus (quoted by L84, L91): at any moment ANY player must be able to play everything; with 49 of 50 AFK the one active player still plays a whole campaign. Nobody is kicked. A host-side decision about peer P may read P's own intent + shared game state and nothing else. A wait on something that ends BY ITSELF is allowed; a wait on a PERSON is not. SCOPED 2026-08-07 (owner): this governs progress AFTER the game has been entered. The LOBBY is outside it — before the game starts there is no play to block, so the host may hold the start until every LIVE peer has readied (L84 arm (c) now asserts the LIVE-only guard instead of the absence of the gate).
  - laws: L26 L64 L71 L82 L84 L91 L94 L109 L118 L119 L120 L122 L129 L136 L143 L145 L150 L151 L155 L85 L86
- **P14 — DRIFT IS THE GATE, AND A LAW ASSERTS AN OUTCOME.** `docs/rail-baseline.txt` + `docs/rail-contract.txt` = committed snapshots; ANY drift is harness-RED, so a field moving Excluded↔covered is a reviewable diff, never a side effect. A law asserts the executed OUTCOME, not the presence of a call, and carries its own non-vacuity.
  - laws: L6 L9 L22 L23 L33 L48 L49 L72 L103 L125 L163
- **P15 — DERIVED, NOT MIRRORED.** Unnumbered in `ARCHITECTURE.md`, asserted in the coverage rules. State a peer can recompute closed-form from already-mirrored inputs is RE-DERIVED locally, never shipped: mirror the ORDER, derive the pose. Inverse holds — what cannot be re-derived (mist) MUST ride.
  - laws: L43 L53 L55 L59 L73 L77 L102 L115 L154
- **P16 — FRAME BUDGET.** Unnumbered in `ARCHITECTURE.md`, asserted by the sliced walk. No unbudgeted graph walk on either peer; the rail never spends a frame the player is using; urgency never outbids local input. Also may not IDLE — a sim fact waits no longer than it must.
  - laws: L50 L74 L100 L153 L154 L156

## Law index

| id | title | P | origin | evidence | guard |
|---|---|---|---|---|---|
| L1 | every list-classed field has an `ApplyList` strategy | P7 | incident | 2026-07-18 resync storm | neither |
| L2 | no `[SerializeCustomCreate]` param unmatched | P6 P3 | principle | ARCHITECTURE.md Verification | neither |
| L3 | no Unity object reaches the blob codec | P3 | principle | ARCHITECTURE.md Verification | neither |
| L4 | leaf / list / blob codec round-trip | P6 | principle | ARCHITECTURE.md Verification | neither |
| L5 | abstract element types classified once the codec turns polymorphic | P6 | principle | boundary-law L-E | neither |
| L6 | every blob-reconstructed element type survives a REAL round-trip | P6 P14 | principle | `Program.cs:757` — "L4 only ever round-tripped a synthetic local class" | neither |
| L7 | dict-delete tombstone / census stay undecodable as values | P6 P7 | principle | — | neither |
| L8 | `SurfaceSeq` monotonic, idempotent, reorder-safe | P7 | principle | ARCHITECTURE.md P7 | neither |
| L9 | `GeoItemDict` coverage is non-vacuous | P14 | principle | re-inclusion; zero silently kills inventory sync | neither |
| L10 | element ORDER and MEMBERSHIP survive the wire | P6 P2 | principle | ARCHITECTURE.md order-vector | neither |
| L11 | no `LocalizedTextBind` rides covered (static belt for DefOwnership) | P3 | principle | boundary-law L-B | neither |
| L12 | `IntentDedup` + `[nonce][op]` envelope | P7 P4a | principle | ARCHITECTURE.md IntentRail | neither |
| L13 | field-codec CRC(host) == CRC(client) after apply | P6 P7 | principle | ARCHITECTURE.md Verification | neither |
| L14 | the DTO-twin coercions stay wired | P2 | principle | ARCHITECTURE.md "DTO twin resolution" | neither |
| L15 | owner back-ref rewired; nested husk named | P6 | principle | husk gate (boundary-law L-B) | neither |
| L16 | an owner post-read waiver is healed, not inherited | P6 | principle | — | neither |
| L17 | a duplicate ROOT key is a walk incident | P2 | incident | per-faction `VehicleID` collision — one aircraft survived, the rest eaten by first-wins dedup | neither |
| L18 | a stage baseline is bound, and the undo floor survives a repaint | P11 | unclear | "the minus button greys out and the spend cannot be undone" — no session named | neither |
| L19 | a loadout gesture emits exactly once; no result-ship from a model patch | P4a | principle | EquipSync generalized into IntentRail | neither |
| L20 | an init-only / unbuilt container applies or fails loudly | P6 | principle | — | neither |
| L21 | a screen whose `ExitState` writes back must be in `UiNativeRepaint.Table` | P11 P8 | incident | `UIStateVehicleRoster:128-131` → `ReplaceEquipments` undid the delta inside the apply | neither |
| L22 | the ordinal VALUE-RECORD codec + the coverage it exists for | P2 P14 | principle | re-inclusion of `AmmoManager.LoadedMagazines` | neither |
| L23 | every static reflection handle the sync layer holds RESOLVES | P14 | principle | `AccessTools.Method` null-on-miss class | neither |
| L24 | a skill-point refund carries its provenance and stays in range | P4a P7 | principle | — | neither |
| L25 | the CRC drift backstop hashes through THE canonical walk and sees a vanished entry | P7 | incident | RCA B1/A4 — a removed path emits no entry and no tombstone | neither |
| L26 | a blocking window pauses once, nobody vetoes a resume, a one-shot is never replayed | P4a P11 P13 | incident | 2026-08-04 live 1+2: host paused alone (21:20); hold-set veto (22:09); cinematic restarted 7× | premise-changed |
| L27 | the event-answer arbiter — first choice frozen for everyone | P7 P5 | principle | pure validator; the race no manual test reproduces | neither |
| L28 | root ownership: ONE instance, ONE root path | P2 | principle | RCA gap B3 trap T1 | neither |
| L29 | the structural DESCEND shape (null↔non-null) | P3 | incident | a finished mission stayed on the client's map forever, no incident line | neither |
| L30 | a Descend create frame carries its type name and its params | P3 | principle | — | neither |
| L31 | the structural ACTOR shape — never blob a MonoBehaviour-bound root | P3 | principle | `ActorComponent.CreateActor` would spawn a duplicate actor | neither |
| L32 | the AIRCRAFT intent family | P4a P12 | principle | RCA gap A2 | neither |
| L33 | a declared root that ships NOTHING | P14 | principle | RCA prediction about the "ST" statistics root | neither |
| L34 | a member whose CONTENT the rail refuses is NAMED, never counted as carried | P1 P6 | principle | husk gate blind to hollow-by-content | neither |
| L35 | the text-bind codec class (key + `_doNotLocalize`) | P3 P6 | principle | re-inclusion of four subsystems (43ac747) | neither |
| L36 | the GeoscapeEvent completion-funnel SET, not one funnel | P4a P3 | incident | one session with only `CompleteEvent` covered — a client bought from the marketplace out of the shared wallet | neither |
| L37 | the UNGUARDED-DEREF class — a half-built window over baked placeholder text | P1 | incident | `[HavenName]` NRE inside `ReplaceEventTokens`; title over "Fasdasdsadasg…" while our log said success | neither |
| L38 | repaint relevance — both over- and under-repaint | P11 | incident | 4-5 fps on `UIStateEditSoldier`; 15 churning kinds rebuilt the perk tree per batch | neither |
| L39 | the geoscape EVENT-WINDOW raise family (0xB6) | P1 P11 | incident | 54 of 94 replayed raises had `Site == null`; pre-join events re-raised | neither |
| L40 | no value leaves a batch unshipped and unannounced | P1 P7 | incident | `GeoscapeEventSystem.EncounterRecords` at 8636 B — the event ledger stopped mirroring permanently | neither |
| L41 | an apply onto a read-only façade succeeds or fails loudly | P1 P7 | incident | `NotSupportedException` on EVERY delta, deduped to one warning; aircraft routes never landed | neither |
| L42 | a client gesture mutating host state has an intent, or is gated | P4a P4b | incident | `ExploreSiteAbility` → `StartExploringCurrentSite` ran to completion on the client's unfrozen clock | neither |
| L43 | no pose leaf of a rail-mirrored navigating actor rides covered | P15 P6 | principle | `GeoNavComponent.NavigateRoutine` closed-form; shipping it can only make the icon STEP | neither |
| L44 | a window the GAME answered itself is not a lost race | P7 P5 | incident | host picker click-dead; clients' copy auto-dismissed ~0.2 s after opening | neither |
| L45 | a late resolution is REPLAYED, not yanked | P5 P11 | incident | v2 dropped `IsNonInteractableWhenSelected` → a hang instead of a replay | neither |
| L46 | every peer shows the queued windows in the HOST's order | P11 | principle | a mirror queueing everything at priority 0 | neither |
| L47 | the peer whose OWN click resolved it is not asked to click again | P5 P11 | incident | the answerer got the observer replay and needed a second click | POSITIVE CONTROL |
| L48 | every window the game pushes has a reviewed answer | P14 | principle | 1 of 9 queue kinds captured, 8 neither mirrored nor declared | neither |
| L49 | the MODAL family: totality over `ModalType` + a non-authoritative client copy | P14 P3 | principle | 43 `ModalType`s behind one view state | POSITIVE CONTROL |
| L50 | the walk cannot go monolithic again | P16 P12 | incident | host log 2026-07-30, 275 ticks: walk p50=40 / p90=60 / max=95 ms; 10-37 fps across forced ticks | neither |
| L51 | a repaint may not take an armed augment selection, nor revert the mirror | P11 P8 | incident | native `OnNewCharacter` stamped the stale visit baseline over mirrored armour; slot locked | neither |
| L52 | click parity — an armed selection leaves its siblings clickable | P11 | incident | ported v1 fix (quarry cbb9b2c): switching part A→B swallowed | neither |
| L53 | site-exploration progress is DERIVED, not mirrored | P15 | incident | before the derivation the client saw NOTHING until the host's counter completed | POSITIVE CONTROL |
| L54 | the host repaints its own applied state; the persistent HUD is part of the screen | P11 | incident | the top-right tracker kept the OLD research text until the player left the screen and came back | POSITIVE CONTROL |
| L55 | the faction objectives list is derived; it must not ride | P15 | principle | the left-hand objectives panel desyncs; abstract element would abort at encode (L-E) — no date, log or measurement | POSITIVE CONTROL |
| L56 | the haven / base / alien-base status twins stay resolved | P2 | principle | alias rows the name conventions cannot reach; failure is silent | POSITIVE CONTROL |
| L57 | a forced re-emit scope names a path the walk can produce | P7 | incident | `EventSync` shipped `"ES.EncounterRecords#<eventId>"` — the element form for an `EntityList` | neither |
| L58 | peer-local containment (hash and order-vector) | P2 P7 | incident | three permanently diverged base roots S#98 / S#99 / S#170, host alone disagreeing, never healing | neither |
| L59 | mist coverage MUST ride | P15 P1 | principle | `_mistData` is a per-frame GPU accumulator — not re-derivable | neither |
| L60 | the persistent HUD is no screen's to silence | P11 | principle | a per-screen `IgnoredKinds` exclusion buying fps out of an unaudited panel | neither |
| L61 | the tac-entry blob is real and its failure is loud | P1 | incident | v1 post-mortem: "493 KB snapshot deserialized empty on a real client", nothing thrown | neither |
| L62 | surface ids are banded, and the band is load-bearing | P1 P2 | incident | v1 RCA 3ff508d — tactical ids at 0xA0-0xA3 ate geoscape traffic for days | neither |
| L63 | client turn control is host-paced; end-turn is an intent | P4a P4b | incident | A1's instant-return coroutine made the client race a whole faction per frame | neither |
| L64 | mission end reaches every peer; nobody is stranded in tactical | P13 P1 | incident | A1 shipped both peers into one battle with no way out; `OpenReturnBarrier` had zero callers | neither |
| L65 | the per-soldier command seam is generic, arbitrated and contained | P4a P5 P8 | incident | v1's in-memory ownership ledger died on every reload, unnoticed for a month | neither |
| L66 | a resolved attack is the host's, verbatim | P3 P6 | principle | five silent-divergence shapes reasoned from the damage path | neither |
| L67 | an actor's life is the host's | P3 | incident | the v1 destroy regression; arm (e) added 2026-08-05 after TFTV's Umbra passed (a)-(d) | neither |
| L68 | an enemy action is the host's; a client only watches | P5 P3 | incident | per-peer-only reactions measured firing on the host and on neither client | neither |
| L69 | inventory commits as a batch; destruction resolves on a peer | P3 P4a | incident | v1 `6617846` locked the screen; `fc661b7` left the destructible mirror dead mission-wide | neither |
| L70 | a level teardown is safe before it starts and loud if it fails anyway | P1 | incident | 2026-07-31 blocker: `UIStateEditSoldier` `ExitState` after the level switch, NRE killed the coroutine | neither |
| L71 | when the load starts, every peer is behind the curtain | P13 P1 | incident | 2026-07-31: 13.0 s of interactive geoscape on every peer but the host | neither |
| L72 | a declared reason may not rest on a retired law | P14 | incident | 2026-07-31 P5 retired the tactical quarantine; two `GeoWindowCoverage` rules kept citing it | neither |
| L73 | a clock write may not zero the level clock's accrual | P6 P15 | incident | 2026-07-31/08-01, 3 instances: every client's aircraft froze and rubber-banded | neither |
| L74 | no unbudgeted graph walk; urgency never outbids local input | P16 | incident | "host smooth, both clients hitch" — `RootCrc` walking a whole root inside one frame | neither |
| L75 | the camera filter stays narrow, and stays a filter | P5 P11 | principle | shared `CameraDirector`; `AccessTools` exact-signature drift | neither |
| L76a | a dropped payload field may not be one the replay itself reads | P6 P5 | incident | 2026-07-31: `Equipment`/`TacticalItem` dropped → NRE on every mirroring peer | neither |
| L76b | a tactical model funnel the UI can click is seamed or declared local | P4a | incident | 2026-07-31: `EquipmentComponent.SetSelectedEquipment` is not an ability; the host then refused the next order | neither |
| L77 | the site/POI seam on a client: one clock and one notification | P15 P11 P3 | incident | 2026-08-04: Explore button missing until a second click; progress bar host-only. `mirrored start 36.08:16:30 … clock (747322.13:10:33)` | both |
| L78 | *(prose-only — no executable arm; see L104)* | P5 | unclear | cited in `TacticalCommandSync`; forced `AnyAIEvaluationAbilityExecuting` TRUE for mirrors | neither |
| L79 | the bottom bar repaints on the screen Exit+Enter is forbidden on | P11 | incident | two peers on one sniper: one fired, the other's AP and ability availability did not move | premise-changed |
| L80 | the order that crosses is the order the player gave | P5 P2 | incident | 2026-08-04, build 816128 B: one JetJump emitted 4× across two turns; soldier flew back a round | neither |
| L81 | a peer re-runs its own vision at the settle; a mirrored stat write fires the native event | P11 P3 | incident | invisible enemy + missing health bar; `0c54378` shipped the repair with no arm | both |
| L82 | peer autonomy over the window queue | P13 | principle | the queue drains only on a click; unbounded list is O(n²) + save payload | neither |
| L83 | the post-mission screens exist on every peer | P11 P1 | incident | user report 2026-08-01 items 3a+3b — outcome modal and resupply vanished from every client | neither |
| L84 | nobody is kicked, and nobody waits for anybody IN GAME — and the LOBBY gate that may wait counts only LIVE peers (`LobbyController.AllLivePeersReady` skips a `Paused` row and a parity-blocked one) | P13 | principle | developer mandate 2026-08-05 + three quorums found in `Multiplayer.Network` (ready gate, LOADED barrier, straggler kick). SUBJECT NARROWED 2026-08-07 on the owner's ruling: readiness lives in the lobby and the no-quorum postulate governs progress only AFTER the game is entered, so the greyed PLAY / NEW CAMPAIGN button is wanted behaviour and `afc111a` is deliberately reversed. Arms (a)/(b)/(d)/(e) are unchanged — the in-game claim is undiminished; arm (c) now asserts the guard that keeps the lobby gate from becoming an infinite blocker, since a dropped peer keeps its roster row forever (`PausePeer`, never `RemoveClient`) | both |
| L85 | a host stream that RESTARTS is applied, not swallowed: `SurfaceSeq.ShouldApply`/`Mark` accept and REWIND on seq 1 against a cursor already past 1, every other backwards seq is still dropped, and `TacticalTurnSync.HandleInbound` names the restart out loud — with both ends of the leave carry path (`OnLocalLeaveBattle`→`HostBroadcastLeave`, inbound→`ApplyLeave`) still wired | P13 P5 | live bug | 2026-08-07 3-peer session: the host pressed RETURN TO GEOSCAPE and parked on "Waiting for players…" for 18 minutes while the ally's mission-results table froze. 0x80 is per-BATTLE and the host's `OpLeave` is by construction sent AFTER the receiver has reset (it exists to reach a peer that has not left yet): client log 21:22:28.540 reset, 21:22:28.658 the trailing leave `Mark`ed the cursor back to the old battle's 12 — so battle 2 (21:35–22:01) applied ZERO 0x80 messages, no turn cursor, no mission end, no leave, and not one log line, while 0x82/0x83/0x84 flowed throughout | both |
| L86 | the peer that ANNOUNCES a load boundary holds its own screen exactly as long as the one it imposed: `CurtainHoldArmed` reads `_loadBoundaryAnnounced`, holds at the real handover row (started FALSE, pending FALSE, announced TRUE), releases with none of the three, and every death of an announced boundary — `Conclude`/`Disarm`NewCampaignBootstrap and a `HostSerializeAndSendCrt` that produced no bytes — reaches `BroadcastLoadBoundaryAbort` | P11 P13 | live bug | 2026-08-07 owner: "creating a new game makes the HOST load twice". `SaveTransferMath` claimed the windows "abut with no gap by construction: LaunchTransfer returns only after Begin() has set the session started" — false: `LaunchTransfer` ends with `timing.Start(HostSerializeAndSendCrt)` + `return true`, and `Begin()` is several yields on, past `ReadSavegameBinary`, `SendBlob` and the host's own `PrepareEntryFromBlobCrt`. `ConcludeNewCampaignBootstrap` runs immediately, so the arm collapses, the fresh geoscape is revealed and interactive, and the blob re-entry drops a second loading screen on it. L94's handover arm was GREEN throughout because it evaluated the ASSUMED row (`sessionStarted: true`) instead of the real one — that arm is repaired, not baselined | both |
| L91 | no peer may be left unable to act | P13 | incident | 2026-08-04 live 3-instance: same travel intent accepted 3× while every resume was vetoed | both |
| L92 | the geoscape modules are found by handle, not by scene scan | P11 | incident | 2026-08-04: activity strip missing on one client until it walked into Research and back | both |
| L93 | the window queue is ordered by the host; a queued window survives a battle | P11 P1 | incident | 2026-08-04, DLL 843264 B: host showed an event, both clients the resupply screen — same 3 windows, opposite order | neither |
| L94 | nobody plays until everybody is in; nobody waits on a peer who is never coming | P13 P1 | incident | 2026-08-04: "whoever finishes loading FIRST … can already act"; `OpenReturnBarrier` had no caller | premise-changed |
| L95 | the squad bar is reactive, and making it so moves no state stack | P11 | incident | 2026-08-04: portrait AP kept the pre-order numbers on every peer that did not click | premise-changed |
| L96 | a settle re-tests vision in BOTH directions; a drop list may not swallow an order | P11 P3 | incident | 2026-08-04, build 843264 B: bandit in fog for rounds; Dash `is DECLARED LOCAL` at 22:23:17.758 | neither |
| L97 | the aim POSE crosses, and nothing else about aiming does | P5 P15 | principle | developer decision 2026-08-04 + `FireWeaponAtTargetCrt:1645` skip = a built-in desync on every shot | premise-changed |
| L98 | a stale value may not win over a fresher one, in the model or on the screen | P7 P11 | incident | 2026-08-04, build 842240 B: 94 host settles, 0 applied, 15305 identical NRE traces | neither |
| L99 | the Kaos marketplace is a shop, not a host privilege | P4a P5 P12 | incident | 2026-08-04 live: buying does nothing on a client; the offer list was never replicated | premise-changed |
| L100 | the rail may not idle, and the clock may not arrive stale | P16 P7 | incident | 2026-08-04 23:22, DLL 842240 B, loopback: ~0.5 s lag; intent 22 ms, broadcast 16-24 ms, apply→apply 112 ms | neither |
| L101 | the reward page ships every row the game draws, as addresses | P2 P11 | incident | 2026-08-04, DLL 842240 B: clients saw resources only, the host also saw diplomatic changes | neither |
| L102 | a contextual offer that was taken is CLOSED, not re-derived | P11 P15 | incident | 2026-08-05 00:17, DLL 875520 B: Explore stays lit over the spinner on every other peer | premise-changed |
| L103 | the native tactical entry is an experiment, and one that cannot be turned off is not | P14 P6 | principle | unproven alternative to the save transfer; the RNG bracket must give the stream back | neither |
| L104 | a shared action starts at one moment; a turn does not end over an unplayed order | P5 P11 | incident | 2026-08-05 00:22-00:47: host finished a shot ahead of both watchers; 3 "camera hint suppressed" lines | neither |
| L105 | a stale stat snapshot may never win over a fresher value | P7 | incident | 2026-08-05 host log: settle #231 ap=2,6 wp=8 → damage → #233 wp=10; client showed +2/-2/+2 | neither |
| L106 | a mirrored modal must rebuild to a real data object on the peer | P2 P1 | incident | the mission brief and the soldier join sat declared, reviewed and un-shipped for months, L49 green | neither |
| L107 | a raise that arrives before its entity ends up SHOWN, not dropped | P2 P7 | incident | the soldier-join reward window the peer never saw, logged as a correct refusal | neither |
| L108 | two Phoenix Point builds must not reach one campaign | P10 | principle | `MultiplayerUI.CoopGuardBlocks` returned false unconditionally at two pre-peer call sites | neither |
| L109 | a queued non-modal window is entered, and does not wedge its queue | P13 P11 | principle | `UIStateAssetDeployment` outside 0xB7; its `ExitState` is the only way out of the queue slot | neither |
| L110 | no spend on the resupply screen escapes the rail; the seam sits where every caller passes | P4a P3 | principle | `ReplenishAll:288` calls `SingleItemReload:234` below the seam — same shape as the `RepairItem` leak | premise-changed |
| L111 | a commit by any peer consumes the shared offer for all of them | P4a P5 | incident | user: "people sit in the trade screen, one of them trades, and nothing changes for the other" | premise-changed |
| L112 | two items of the same def in one container resolve to distinct items | P2 | principle | A7's `(actorKey, kind, defGuid)` address; a full and a spent clip share a def | neither |
| L113 | no identity question on the rail is asked with `==` | P2 | incident | found by hand 2026-08-05 (marketplace vehicle pick, tactical inventory address); `BuildBattleKeys` took the Release harness down | neither |
| L114 | two Multiplayer mod versions must not share a campaign | P10 | principle | sibling of L108; the version rode the join as one anonymous "Mod version differs" line | neither |
| L115 | state that says "finished" beats a local animation still playing | P15 P11 | incident | 2026-08-05, 3 instances: host finishes the POI, clients still draw it unexplored; never self-corrected | premise-changed |
| L116 | a repaint rebuilds every panel the open container view shows | P11 | incident | 2026-08-05: item moved between two adjacent soldiers appears on one side only; personnel bar flickering | premise-changed |
| L117 | a restored window whose subject has resolved is not restored | P11 | incident | 2026-08-05: the played mission's START-MISSION window came back with the history | premise-changed |
| L118 | a client's loading bar shows the host's real progress | P11 P13 | incident | 2026-08-05: "on the clients there is an EMPTY loading bar"; P1 of 3 phases had nothing | premise-changed |
| L119 | the ready count is a LABEL, and it must stay one | P13 | principle | developer framing: "ONLY a visual indicator … doesn't oblige anything" | premise-changed |
| L120 | when somebody goes, everyone is told, and told the truth | P13 P1 | principle | developer ask 2026-08-05; `b48756e` made "gone" and "left" two different facts | premise-changed |
| L121 | a contained spawn is inert to the GAME's own enumerator | P3 | incident | 2026-08-05 live 3-instance: FINISH hung forever; `TacticalActor.OnExitPlay` NRE broke the level coroutine | premise-changed |
| L122 | one session entry into a tactical level, one load per peer | P13 P1 | incident | 2026-08-05 live: one 995,978 B blob, then a second ~996 KB transfer reloaded only the clients | premise-changed |
| L123 | an actor the host is not animating is still corrected; a refused order says so | P7 P5 | incident | 2026-08-05 live: cloaked enemy diverged; 5 winds-and-cancels in 45 s with no on-screen reason | premise-changed |
| L124 | two windows caused by two rail messages open in the same order on every peer | P1 P11 | incident | 2026-08-05: research completion + geoscape event 384 ms apart, shown in the OPPOSITE order | neither |
| L125 | a patch we declare is a patch that BINDS | P14 | incident | 2026-08-05: RailCheck green, mod DEAD in the player — `PatchAll failed … <ReturnFire>d__321::MoveNext()` | neither |
| L126 | a transpiler of ours substitutes a computation; it never writes one down | P6 P3 | incident | 2026-08-05 `b3d9269` (reverted): forced `stepOutNeeded` into every mirror; input went dead, harness green | neither |
| L127 | a confirm either activates or says why (arm (d), "a tactical clone must silence its raycast and name a built face", WITHDRAWN 2026-08-08 — it MANDATED the edit that killed the co-op ready button; the inverse now lives in L220 `ready-face-disarmed`) | P1 P11 | incident | 2026-08-05: Overwatch confirmed for 32 s on a client, no activation line ever logged | premise-changed |
| L128 | a baseline is a promise about ONE level | P6 P1 | incident | 2026-08-05: site COMPLETED on the host, still an active quest site on the clients; f15969 BASELINE swallowed it | premise-changed |
| L129 | de-blockering a popup must not delete it | P13 P11 | principle | L91's cure also removes `TryShowContextHint`; premise measured on a client 2026-08-05 21:58 | premise-changed |
| L130 | a hint triggered on one peer is displayed on every peer, exactly once | P11 P5 | incident | 2026-08-05 live: Umbra panel on ONE client; elite/gang panel on a DIFFERENT single client | premise-changed |
| L131 | every peer ends with the same status set, and the same passenger roster | P3 P7 | principle | field-class audit: statuses rode nothing; `VehicleComponent.Passengers` was a replay artefact | premise-changed |
| L132 | an order may only name a target the host itself offers | P5 P3 | incident | 2026-08-06: every client overwatch refused for four days; host settle byte-identical to seq=20 | premise-changed |
| L133 | a mirrored ability always reaches a terminal state, and it is the host's — on the CLIENT by the settle ceiling, on the HOST by the door: `Validate` refuses an intent for a non-rider before anything native runs, and `HandleActivate` is what asks | P7 P5 | incident | the 2026-08-01 bash NRE — coroutine chain broken, actor never left `ExecutingAbilities`, soldier bricked. REPAIRED 2026-08-07: arms (a)-(c) were GREEN through the 2026-08-06 frozen ambush and were CORRECT — the client's 10s ceiling fired on schedule. The hole was WHOSE ceiling: `SettleMustBeForced` is consulted from `ClientTick` and the host runs none, yet the host replays a client's intent through the same native `Activate` with the same wire parameter, so it took the identical `OpenCrateAbility` throw with nothing behind it. One stuck actor makes `IsWaitingForActiveAbilitiesAndMapUpdate`:864 true for the whole map (L145's narrowing is the player's own turn only) and every `TacticalLevelController` turn-pump wait spun forever — the enemy turn that never started | premise-changed |
| L134 | the new-campaign bootstrap's one shot is spent by an outcome, never an evaluation | P1 | incident | 2026-08-06, 3 instances: campaign created then nothing forever; "autosave capture failed" | premise-changed |
| L135 | one producer per mirrored window | P1 P11 | incident | 2026-08-06: three TFTV intro popups + cutscene answered on the host, then again on every client | premise-changed |
| L136 | a peer the session knows is a peer the transport will broadcast to | P13 P1 | incident | open beta 2026-08-06: real remote player on "Connecting…" ~75 s, no exception anywhere | both |
| L137 | "this soldier's turn is over" is the host's answer, on every peer | P3 P7 | incident | the 2026-08-06 overwatch desync (L132) left a permanent one-sided `HasEndedTurn` | premise-changed |
| L138 | a client-writable covered leaf has an intent seam, and what arrives is re-derived | P4a P11 | principle | coverage-gap-iv audit: `CharacterIdentity` 15/15 covered, `grep -ri customiz src/` = one hit | premise-changed |
| L139 | a mirrored order lets go of the local UI that was holding that soldier | P11 P5 | incident | 2026-08-06: 797 NREs then a native `Crash!!!`; plus the 10-second input stall | premise-changed |
| L140 | a mirrored target is resolved, never approximated | P2 P5 | incident | 2026-08-06 crash: `PositionToApply = (-806.8, 66.4, -615.6)` on a ±20 map; NaN tail four times | premise-changed |
| L141 | a resolved mission-start encounter leaves every live peer on the squad screen | P5 P11 | incident | 2026-08-06 two peers: the host that LOST the event race went straight into the battle | premise-changed |
| L142 | after a reveal, no input-set override left by the loading path is still held | P1 P11 | incident | 2026-08-06: full fps, zero exceptions, uGUI clicks — everything else dead. Repair fired once at 21:17:23.187, never again | premise-changed |
| L143 | the curtain-arm broadcast precedes the host's own load on the wire | P13 P1 P12 | incident | 2026-08-06: host curtained 21:16:58.750, client 21:17:10.126 — 11.4 s of one screen loading alone | premise-changed |
| L144 | a peer "headed for" the squad screen has nothing of its own in front of the queue | P5 P11 | incident | 2026-08-06 second session, DLL 998912 B: the host never entered `UIStateRosterDeployment`; client took 9 frames | premise-changed |
| L145 | a peer keeps control of the actors it is not commanding | P13 P5 | incident | 22:22:30 build: `released this peer's UI from Soldier_3 … it was held in UIStateWaiting` | premise-changed |
| L146 | an actor executing an order takes no command from another peer | P5 P7 | incident | report: "the character freezes in place … and then it teleports"; reject reached the log, never the screen | premise-changed |
| L147 | a visual a status owns is torn down on the RECEIVING peer | P11 P3 | incident | 2026-08-06: overwatch cone hangs on the client for the rest of the battle; 3 `GetWeapon()` NREs | premise-changed |
| L148 | a mirrored identity change repaints the open squad/customization screen | P11 | incident | 2026-08-06: rename and recolour needed a full screen exit and re-entry; `no per-kind mapping for CharacterIdentity` | premise-changed |
| L149 | both helmet toggles are the same argument to the same funnel | P11 P5 | principle | the TOGGLE is a view argument (`CharacterIdentity` has no helmet field) — but a mod PERSISTING its preference into a campaign event variable makes that half replicated, and a client's write now crosses as an intent (0xB4 op 2) | premise-changed |
| L150 | no sequence of concurrent skill confirmations drives the balance below zero | P13 P7 | incident | 2026-08-06 exploit, verbatim: both peers confirmed, BOTH skills learned, points went NEGATIVE | premise-changed |
| L151 | a peer waiting on another peer's load sees that load advance | P11 P13 | incident | 2026-08-06: first `RosterProgress SEND []` 14.2 s after the boundary, and empty | premise-changed |
| L152 | a chat line delivered to a peer appears exactly once | P7 | incident | 2026-08-06: "instead of one message he received TWO … it happened only ONCE" | premise-changed |
| L153 | the host↔client clock phase error has a seam that can read it, and that seam writes nothing | P16 P7 | incident | 2026-08-04 23:22 three-instance session: client-derived aircraft/exploration trail the host, and all three peer logs carry not one `TimeAnchor` line | both |
| L154 | the departure and exploration-start seams are wired to the urgent flush, and a mid-cycle flush is still served urgently | P16 P15 | incident | 3-instance sessions: geoscape flight and exploration spinner trail the host by a steady ~0.25-0.5 s while gesture-driven windows/orders are instant | both |
| L155 | each way of joining keeps the transport it names; a pasted code never silently tries Steam | P13 P12 | incident | the owner's report: pressing "copy code" in the lobby and handing that code to the other player still brought the session up over Steam | both |
| L156 | an in-inventory reorder repaints an ALREADY-OPEN equip screen — order stays rail state, the kind is never declared irrelevant, and the reseed still reaches the widget rebuild | P11 P16 | incident | HANDOFF §5d symptom 1: two peers on the same soldier, one rearranges items inside the inventory, the other "sees NOTHING until he closes and re-opens the screen" | premise-changed |
| L157 | applying a mirrored TACTICAL inventory batch marks the open container screen on both apply paths, and the repaint it asks for never re-commits on the peer that is only watching | P1 P3 | incident | in-mission report: two peers on one soldier (and on the neighbour's panel the trade view shows), a slot↔backpack swap by one leaves the other's open inventory unchanged until it is closed and re-opened | both |
| L158 | a presentation seam never blocks, writes a `[SerializeMember]` leaf, or throws into game code — and a latency reading reaches the panel and NOTHING else: `PingTable` is unreachable from `DiffEngine` / `GenericApplier` / `SurfaceRouter` / `TimeAnchor`, carries no serialized member, and is no rail root. Seam set: `UiEventMap`, `UiNativeRepaint`, `OpenUiRepaint`, `ResearchSync`, `PingTable`, `PlayerPanel`, `PingMarkers` | P4c | principle | P4c was cited by nothing executable (`grep -rn "P4c" tools/RailCheck src` = 0 hits) while HANDOFF §5e loaded it with two new presentation features; the RTT arm pre-empts this repo's dominant failure — a diagnostic number silently becoming replicated state, the shape L153 records as "fixed ten times" while nobody could read it | both |
| L159 | the co-op player panel is FED and admits what it cannot report: the per-peer `Paused` / `TacReady` flags survive the roster round trip, `PingTable.GetPingMs` answers an unmeasured OR a stale peer with a NEGATIVE number (the panel's only "draw the em-dash" signal), and the bar meter's thresholds are the recon's <60 / <120 / <250 / else | P4c | principle | L158 asserts what a presentation seam must not DO; nothing asserted that this one is fed. Both failure modes are silent — a truncated trailing block renders every remote player permanently not-ready with no log line, and a stale SRTT renders as a plausible live number for a peer nobody has heard from (L145) | both |
| L160 | an arriving PING points and does not drive: the ARRIVAL CLOSURE (`Show`/`ShowLocal` plus everything they call inside the class — narrowed from "every method" 2026-08-08, because the off-screen arrow now takes a CLICK that centres the clicker's own camera, and a watcher who never clicks never moves) reaches no camera mover (`GeoscapeView.ChaseTarget`, `Hint`, …), no view-state entry (`SwitchToState`, `EnterState`, any `To*State`) and no selection writer (`SetSelectedActor`, `SelectActorAndVehicle`, `TacticalView.set_SelectedActor`) — and the hotkey is still POLLED, so it works in deployment too | P4c P11 | principle | the L97 extension HANDOFF §5e asks for. L158's seam-set arms cannot state it: moving a camera, entering a state and selecting an actor are all things the game's own UI does legitimately, so none writes a `[SerializeMember]` leaf and none is a suppressing prefix — every arm here would be green under L158 while a watcher mid-shot had his camera flown away by somebody else's key press | both |
| L161 | nobody is ever ASKED to end the round while a soldier can still act: `TacticalAutoEndTurn.CanStillAct` answers yes for an alive, on-field, undisabled, off-standby actor holding AP and no for every disqualifier, the fold is over the SHARED `TacticalFaction`, and both halves bind — a postfix on `TacticalFaction.ShouldAutoEndTurn` (the raise) and a bool prefix on `EndTurnPromptActionDef.PromptCallback` (the answer, re-checked because the box is clicked seconds later while every other peer keeps playing) | P3 P13 | live bug | 2026-08-07, 3 peers: the host was shown "everyone is out of action points, end the turn?" while his ally still had AP on two soldiers and a vehicle, pressed OK, and the ALIEN TURN STARTED — nobody pressed End Turn, the box did. The native fold (`TacticalFaction`:521) never reads AP; its whole notion of finished is `HasEndedTurn` = `HasAbilityTrait("terminal")`, the one per-actor turn bit the peers demonstrably disagree about (L137, and the settle reconciler's own warnings). NOT a quorum: a deliberate End Turn press still takes everyone | both |
| L162 | a peer's camera always comes BACK: no type in the Multiplayer assembly patches `CameraDirector.RemoveHint` / `RemoveHints` / `ForcedReset` at all — the three ways to reach `Evaluate()`, which is the only thing that can lift a chase whose `LockCameraMovement` is set — while the push filter `CameraAbilityHintGate` survives | P4c P11 | live bug | 2026-08-07: two players acting at once left one player's camera locked onto a soldier, unmovable. `PlanarCamDef`:22 says it in its own tooltip ("chasing a transform has to be manually deactivated or the camera will be locked"); `PlanarScrollCamera`:468 eats every pan, :838 refuses to stop a locked chase, :747 self-ends only a null `ChaseTransform`. L75 arm C was GREEN throughout because it asserted the pop gate BOUND — binding was the defect; that row is repaired, not baselined | both |

| L163 | a notification is reviewed on the MAP: `WindowOrder.HoldsForOpenScreen` HOLDS every request below `TransitionPriority` (the event family 0/10/15 and the resupply rank) while the local peer's current view state is a screen it opened, DRAINS on every declared map state and on a null state, and never holds a TRANSITION (int.MaxValue squad screen / outcome modal, 100 cutscene) | P14 P11 P13 | live bug | 2026-08-06 owner: "popups now YANK the host out of whatever menu he is in and BLOCK him until he dismisses them — the design is the opposite, popups ACCUMULATE and are reviewed when the player reaches the geoscape". Vanilla needs no such rule because every section screen STOPS THE CLOCK on entry (`UIModuleGeoSectionBar`:119/:135/:148/:160, `UIStateResearch`:22, `UIStateManufacturing`:51); the 2026-08-04 pause rework made resume unconditional from any peer, so another player's play button now runs the sim behind an open screen and `ProcessQueriedStateSwitch`:71 pushes on top of it. The clock used to hold the queue and nothing replaced it. L48/L49 assert WHICH windows cross, never WHEN one may take the screen | premise-changed |
| L164 | the post-mission resupply gate is ASKED AGAIN once the returning peer's own state has arrived: `SyncEngine.Tick` drives `ReplenishSync.ClientArrivalTick`, which re-asks the GAME's `GetMissingItems()` and raises through the GAME's `QueueReplenishState` under a finite named ceiling, armed off `UIStateInitial.EnterState`'s own `_params.LastMission` and released at teardown | P11 P1 | live bug | 2026-08-06 client log: not one `Queuerd state switch … UIStateReplenish` in the whole session while the same branch's custom-mission arm fired. 76980f2's `CompleteSilently` fix was NOT undone ("CLIENT stamped mission outcome" at 21:22:39.119) — what it asserted away is a RACE: a client's post-battle geoscape is the host's MID-TACTICAL save, so at `EnterState` (frame 52446) every soldier still carries the full pre-battle loadout and `GetMissingItems()` is empty; the host's `PostMissionReplenish` writes land on 0xAC at frame 52448. "All already mirrored" was true; ARRIVED was not | premise-changed |
| L165 | one peer's sighting reaches EVERY peer: the host's 0x8A relay is unconditional and reached BEFORE the display choke (IL order, in `HintMirror.HandleInbound`), a `Show` that did not register gives the name back through `Forget`, the choke still dedupes and `Reset` still releases the battle, and the only sender is still `Capture` so an unconditional relay cannot echo | P5 P11 | live bug | 2026-08-06 owner: the elite-spotted panel does not appear on every peer, "fixed several times before". The client log holds ONE `[MP][hint]` line for five battles and zero "triggered on this peer — mirrored to every peer". L130 was GREEN throughout because it executes `ShouldSend` and asserts the DEDUPE — and the dedupe WAS the bug: the same set gated send, display AND relay, so the host's own eyes (it sights first, it runs the sim) swallowed a client's 0x8A and the third peer got nothing; a delivery a peer could not show silenced that peer's own later sighting for the rest of the battle | premise-changed |
| L168 | a player who RE-SELECTS a soldier another peer is driving cannot restart the move-range sweep: `TacticalCommandSync.MovePollMustBeWithheld` is a STANDING gate on `MoveAbility.GetTargetsData` itself, asking the engine's own precondition with the engine's own `PanicAbility`/`AIEvaluationAbility` exceptions, and answering an EMPTY non-null sweep -- while this peer's own order, an idle actor and a solo game keep the engine's behaviour verbatim | P11 P16 P4c | live bug | 2026-08-07 owner's client log: 470 of the engine's own `GetTargetsData() shouldn't be called while ... is executing abilities` over 4.5 minutes on TWO host-owned actors. L139's `ReleaseLocalUiHolding` is a ONE-SHOT gated on `view.SelectedActor` being that actor at the instant the mirror lands, so a re-selection walks straight back into `UIStateCharacterSelected`, whose `ValidMoves`:153-160 polls every frame it draws. The engine logs and carries ON with no early return, into `GetTargetsDataInRange`:207-236 -> `MoveAbilityTargets.CacheTargets`:17-28, recalculating a path request mid-navigation and assigning the STATIC `NavigationSettings.Defaults` so `PathRequestPostProcess` is off for the whole level: 403 frames, 0.14 of 1.41 units, force-settled at the ceiling | premise-changed + POSITIVE CONTROL |
| L169 | an order aimed at a FREE POINT is not held to a list the game itself discards: `TacticalCommandSync.GroundTargetingExemptsTheOrder` reproduces `TacticalViewState.IsTargetingGround`:200-219 verbatim (including the `Sphere` and no-weapon arms) and `TargetIsOffered` consults it BEFORE enumerating, narrowed to `ShootAbility` exactly as `UIStateAbilitySelected`:199 narrows it | P5 P3 | live bug | 2026-08-07 owner: on a client the grenade arc drew GREEN, the throw did nothing, the soldier twitched. The host refused it with "the chosen target is not one this ability offers on the host" five times on one soldier in 25 seconds, at `(1.9, 0.1, 10.4)` / `(1.9, 0.1, 10.3)` / `(2, 0, 10.6)` — tenths of a unit apart, i.e. a human nudging a free cursor. Our gate held every `ShootAbility` to `GetTargets()` (a grid floor-cast sweep), but the engine THROWS THAT ENUMERATION AWAY for a ground-targeted shoot (`UIStateAbilitySelected`:198-202 → `Enumerable.Empty`, :468-470 enters `UIStateShoot` with a NULL valid list) and decides the arc on `GetShootTarget(...) != null` over the raw cursor point (`UIStateShoot`:1180-1182). The reject then forced a settle whose `CancelActions` tore the wind-up out — that is the twitch. Cone weapons and free-aim/FPS shots share the root. L132 was GREEN throughout and could not have caught it: it executes `ChoiceIsOffered`, which is correct and unchanged — a free-aim throw is simply "published TRUE, matched FALSE" for a legal order | premise-changed + POSITIVE CONTROL |
| L170 | an open container panel survives a VEHICLE and is still actually rebuilt: `TacticalUiRepaint.VehicleSlot` strips the null AND keeps the mounted module (run, not read: fed the `{null, item, null}` a bare vehicle produces), answers EMPTY-not-null so the slot is cleared rather than skipped, and `RepaintContainerView` routes a vehicle through `UpdateVehicleData`/`UpdateVehicleSecondaryData` — the same split the game's own `UpdatePrimaryDataCommand`:131 / `UpdateSecondaryDataCommand`:182 perform | P11 P1 | live bug | 2026-08-07 owner bought a vehicle; in-mission its inventory NRE'd at `UIInventoryList.GetFirstAvailableSlot` (Player.log:32072), then THREE MORE times uncaught at `GetTotalItemWeight` in the owner's own drag/drop and panel-open (:32098/:32126/:32193). `GetVehicleReadyItems`:624-632 returns three `FirstOrDefault`s (:621/:611/:606), so an unmounted weapon IS a null; we handed that raw list to the FLAT `UpdateSecondaryData`:767, and `SetItems`:120 installs it BEFORE `ApplyFilter`:125 throws — so our one caught exception left `SecondaryReadyList` poisoned and every later native `GetSecondaryWeight`:972 threw in the player's hands. TFTV is in the stack but not the cause: its `UIInventoryList_UpdateList_patch.Prefix` reproduces native `UpdateList`:595-597 exactly. L116/L157 assert the rebuild's shape and that something ASKS for it — neither says the rebuild must be the one the ACTOR TYPE requires | premise-changed + POSITIVE CONTROL |
| L171 | a CONSUMED marketplace offer is purchasable on NO peer, and a changed offer list repaints the open shop without a re-enter: the buy intent carries the offer KEY and nothing positional, the host RESOLVES it over its own live list (`MarketplaceSync.ResolveOffer`, cheapest row on an ambiguous key so no price rides the wire), `HostBroadcastOffers` is the ONE funnel every host-side list change repaints from, and a refused click answers IN THE PANEL — the family reconverge re-pushes the host list so the clicked row disappears — never on `IntentRail.Reject`'s notify path, which L123 forbids for an ordinary refusal | P11 P3 P13 | live bug | 2026-08-07 owner: client bought a soldier, every other good vanished, buttons dead, only re-entering the shop recovered it, and an already-sold offer could be clicked again. Log: three intents for ONE offer id (nonces 143/144/145), then `buy: row 10 is outside the host's 4-offer shop` and `row 0 is 'I:29998034…' here, not the 'U:{3FBC2BB0…}' that was clicked`. L99 was GREEN throughout — it asserts the click is CAPTURED and the mirrored list is repainted, never that the address survives the list it was read off | premise-changed + POSITIVE CONTROL |
| L172 | a scrap cart fits the POOL it is taken from: `ManufactureSync.ScrappableCount` reproduces `StripPartialMagsFromScrapStorage`:1081-1082 (run, not read — 4 of a 5-stack with a partial clip, 5 of a full one, 5 of an ammo-compatible def), `CartLiveCount` and `PruneCartsToLiveStorage` both take their ceiling from it instead of the raw stack, `ResyncScrapSnapshot` REBUILDS the pool before it clamps or stages against it (IL order), and a cart the pool cannot pay for is named out loud before the subtract that underflows | P3 P1 P13 | live bug | 2026-08-07 client, 31 engine errors in twelve seconds: `Trying to remove 5 items with 36 charges but only 4 items with 32 charges available in PX_SniperRifle_AmmoClip_ItemDef` (x15), `PX_Pistol_AmmoClip` 5/35-vs-4/32 (x11), `PX_AssaultRifle_AmmoClip` 17/600-vs-16/576 (x2) — every one off by EXACTLY one magazine. NOT a mirroring fault: `ItemStorage` is covered 2/2 and `GeoItem` 1/1 on the baseline and `CommonItemData._charges` rides. Both numbers were local — the raw faction stack vs the scrap screen's pool, which `Init`:363-371 ends by stripping one partial magazine per def out of. A host cannot overdraw (native `AddToScrapQueue`:1109-1116 moves items OUT of the pool and throws when empty); a client's click is blocked and validated by `CartLiveCount`, which read `Count` off the stack — so a client staged a magazine that did not exist for it, `snapshot.RemoveItems(cart)` underflowed, and `ItemStorage.RemoveItem`:87-89 then dropped the def off the screen entirely. The only report was the GAME's bare `Debug.LogError` at `CommonItemData.Subtract`:224 — a def name, no peer, no cart | premise-changed + POSITIVE CONTROL |
| L178 | an ability whose own playback cannot READ what the seam ships never rides it: for every `TacticalAbility` subclass in the game, every hard cast (`castclass`/`unbox.any`) applied directly to `PlayingAction.Param` must accept a `TacticalAbilityTarget`, or `TacticalCommandSync.LocalAbilities` declares that ability local — `isinst` (`param as T`) deliberately not counted, and `MoveAbility`/`ShootAbility` cast just as hard and must stay riders | P1 P5 P7 | live bug | 2026-08-06 ambush: `OpenCrateAbility.OnActorAbilityExecuted`:96 activates with a `CrateComponent` and `OpenCrate`:55-58 opens with `(CrateComponent)action.Param` and dereferences it. The codec has no field for that parameter, so the wire carried `<no target>` (mirror 23:18:19.606 `Parameter: <>`; client intent 23:20:24.464 nonce=113), the cast landed on null, `<OpenCrate>d__10.MoveNext` threw and the engine printed `Broken coroutine call chain`. A torn coroutine never reaches `CompleteAction`, so `ClearPlayingAction` never runs and the actor never leaves `ExecutingAbilities` (`holding the settle … 2s/4s/6s/8s`, forced at ten). On the HOST there is no ceiling at all, `IsWaitingForActiveAbilitiesAndMapUpdate`:864 folds over the WHOLE map, and the enemy turn never started — no enemies, Alt+F4. L133 was GREEN throughout and is repaired, not baselined | premise-changed + POSITIVE CONTROL |
| L179 | a loot container's inventory opens on the peer whose view is HOLDING that soldier and on no other: `TacticalContainerOpen.InventoryWindowMayOpen` refuses only the watcher case (a solo game, no view, and the holding peer are positive controls in the same arm), the seam is a bool PREFIX on `InventoryAbility.Activate` that actually consults it, and both game premises are pinned — `InventoryAbility.Activate` still reaches `ToInventoryViewState`, `UIStateInventory.PrimaryActor` still reads `TacticalView.SelectedActor`, and `OpenCrateAbility` still reaches `InventoryAbility` | P4c P5 P11 | live bug | 2026-08-06 ambush: a CLIENT's soldier walked up to a crate and the container window opened ON THE HOST, empty, showing the host's own soldier's kit — and in reverse. Nothing we relay does it: `InventoryAbility` has been declared local since A6 and the log proves the drop fired. `OpenCrateAbility.AbilityAdded`:38-42 subscribes `AbilityExecutedEvent`, so `OnActorAbilityExecuted`:70-97 fires on every peer whose copy of that soldier finished an `IMoveAbility` — a MIRROR does — and `OpenCrate`:63 ends at `GetAbility<InventoryAbility>().Activate()`, which calls `View.ToInventoryViewState()` with no test of whose soldier it is. EMPTY because `UIStateInventory.PrimaryActor`:273 IS `Context.View.SelectedActor` and `GetInventoryComponents`:663-677 lists the containers in range of THAT actor. Vanilla already relies on the two matching; the gate asserts the game's own precondition rather than an ownership rule (P5 has none) | premise-changed + POSITIVE CONTROL |
| L191 | a peer is never told to wait for, nor made to discard, state it AUTHORED: `MissionEncounterNav.NoDeploymentReason` gives the host and a client different reasons and never tells the host it is waiting for arrival, while `RestoreDropsResolvedSubjects.DropsRestoredWindow` drops a restored window only when the blob is another peer's AND the kind is Mirrored | P13 P11 P1 | live bug | 2026-08-07 HOST log: `[MP][mission] deployment for 'PROG_NJ0_MISS' at S#104 NOT opened — the host's mission has not arrived on this peer yet` printed BY THE HOST ABOUT ITSELF (:2105, 23:10:31.254), against its own `HOST answered … peer=1` 23:10:12.981, `structural create 'S#104…ActiveMission'` 23:10:13.052 and `structural DESTROY` 23:10:26.624 — it minted that mission 17 s earlier and destroyed it 4.6 s before the click, so nothing would ever land. Second half, ruled NOT a divergence and pinned so nobody "fixes" it: host `3 entries … 3 kept` (:1382) vs client `3 entries … 0 kept — 3 dropped` (:367) from the SAME save is CORRECT — the host raised IntroBetterGeo_0/1/2 itself 23:09:06.43 and its autosave carried them, the client had them as 0xB6 raises 23:09:13.96 and replayed them 23:09:28.6-29.1, so both ended with three. One live copy per peer, never equal restore counts. L135 asserts the two halves share one producer signal but cannot see the ROLE SPLIT, which read its conditions inline — deleting the producer test while keeping the call for the log line would have left it green | premise-changed |
| L190 | a rate change is not drift, and a clock jerk is visible on the peer it jerks: `TimeAnchor.Drifted` reads the anchor's own latched `Paused`/`Scale` before answering, `ApplyIfTouched` writes a log line, and it still calls `Rebase` | P15 P14 P7 | live bug | 2026-08-07 HOST log, the only two ERRORs this class produced in 16 min: `TimeAnchor: 5 DRIFT re-latches in 10 s (threshold 4)` at 23:10:09.971 and 23:10:50.724 — both FALSE. `Drifted` extrapolates the whole interval since the latch at the rate read NOW, so on the one walk cycle a pause/resume/speed lands it measures the clock against a rate the clock never ran at: a RESUME after a P-second pause reads `rate × P` against a `rate × 0.5` tolerance, a PAUSE reads rate 0, collapsing the tolerance to its 5 game-second floor against the whole running stretch — over threshold both ways. The second alarm's window (realtime 401.5-411.5) holds exactly five `[MP][pause]` transitions (402.264, 402.740, 408.488, 409.409, 409.817, a vehicle-order storm) and the other ~15 min of geoscape are silent: the alarm was counting speed clicks. `a4a368a` added the `driftLatch` flag for precisely this exclusion ("a pause/speed latch is a user gesture") and missed it, because the flag was computed by a rate-blind predicate hoisted above the Paused/Scale comparison; **L73 arm E was landed by that same commit and stayed GREEN throughout** — it asserts an inequality (threshold below the latch ceiling), which says nothing about what gets counted. Second half: the accused peer logged NOTHING. `ApplyIfTouched` moves the client's level clock through `Rebase` and wrote no line, on 2026-08-07 and on all three peer logs of 2026-08-04 23:22 — the blindness `TimeAnchor`:76-99 and `ClockPhaseDiag` both open with, and why ten fix attempts were aimed at a quantity no log carried. The OFFSET stays unasserted (runtime quantity, same boundary L153 draws); the LINE is asserted | premise-changed |
| L175 | the open-screen hold is asked about the WINDOW it is about to serve, not only its priority: `WindowOrder.HoldsHead` is executed over a REAL `GeoscapeViewStateSwitchRequest` and HOLDS a queued `UIStateRosterDeployment` on a player-opened screen while an ordinary `int.MaxValue` request, and a request with no state at all, still DRAIN — and the predicate reads no peer, roster or ping table | P14 P11 P13 | live bug | 2026-08-07 owner: a partner pressed "start mission" and the HOST was ripped out of the RESEARCH screen onto the mission-start window. `19af84c`'s `HoldsForOpenScreen` read the request's PRIORITY alone, and `ToDeploymentState`:596 queues the squad screen at `int.MaxValue`, so the yank was EXEMPT BY DESIGN — L163 arm (c) asserted that exemption in words ("int.MaxValue … DRAIN even on a screen"). L163 is REPAIRED (its arms now carry an ordinary window; new arm (e) executes the carve-out both ways); this law adds only what a green L163 cannot see — that the DRAIN GATE passes the head's own type, and that widening the carve-out past the one named state would hold a cutscene or the game-over state behind a player with nothing able to release it. The double-queue hazard the hold creates is L141's `already-headed-answered-off-the-books`, not a second copy here | premise-changed |
| L176 | a window the HOST RAISED is answerable while it is still QUEUED, and the screen that answer opens re-asks the game's own question at open time: `WindowQueueSync.MayAnswerQueued` accepts a named queued window only when it is not also the current one (which `FinishDialog` owns), `HandleAdvance` reaches `AnswerQueued`, that path takes the request out of `_viewStateSwitchRequests` and invokes the host's OWN `_dialogHandler` while calling no `Finish*` at all, and `DeploymentRosterRefresh.Prefix` calls `GeoMission.GetDeploymentSources` + `GetDefaultDeploymentSetup` | P11 P1 P4c | live bug | 2026-08-07 ambush: the deployment-prep window NEVER APPEARED for the host, and when he reached it there was NO START MISSION BUTTON. Two roots. (1) The `GeoAmbushBrief` is `OpenModalPersistent(...,0)` — review priority — so `19af84c`'s hold correctly kept it QUEUED behind the host's research screen, and `HandleAdvance` reads `_currentStateSwitchRequest` and only that: the client's 0xB9 answer was logged "did NOT apply", so `FinishDialog` → `ModalResultCallback`:799 → `LaunchMission`:1043 never ran and no squad screen was ever queued. `WindowOrder`'s own doc had ruled this exposure acceptable in writing. `FinishDialog` cannot be the fix — it runs `FinishCurrentStateSwitch`:116 → `SwitchToPreviousState` on a state never pushed, popping the screen the player is in. (2) `UIStateRosterDeployment`'s CONSTRUCTOR (:74-82) computes both inputs of `CheckForDeployment`:372 at QUEUE time (`ToDeploymentState`:595), so a stale snapshot is `SetInteractable(false)` over an empty roster — the L164 race, one screen over, and L175 just made the gap minutes long | premise-changed |
| L177 | the deployment countdown completes ON ITS OWN and one peer's cancel stops it for everyone: `DeployCountdown.RunsNative` is executed over all four cases (solo and a client run native, the host's uncommitted launch is refused, the released one passes), `DecrementDue` stays a comparison of two clock readings, NO method of the class calls `SessionManager`/`PingTable`/`PeerListEntry`/`LobbyController`/either panel, `HandleCancel` forwards straight to `Cancel` with no per-peer test, nothing on the cancel path calls `GeoMission.Cancel`, and `MissionSync.CaptureLaunch` + `SyncEngine.Tick` actually consult the gate and drive the clock | P13 P11 P12 | principle | owner's design 2026-08-07: pressing Deploy throws every peer into the battle with no way for someone mid-re-arm to say wait. A five-second overlay with a CANCEL button is one misreading away from the ready-vote the standing mandate forbids, so the difference is pinned mechanically rather than left to intent — PROCEEDING IS THE DEFAULT AND NEEDS NOBODY TO ACT, and a veto is one peer's, applied immediately, never tallied. Rides no new surface (the geoscape band 0xA0-0xBF is fully allocated, the wall L99 already hit): the arm is mod-state root `M#deploy` on the 0xAC value rail beside `M#cart`/`M#mist`, the veto is op 2 on 0xB8 | POSITIVE CONTROL + premise-changed |
| L173 | the curtain gate's EVIDENCE LINE names every input of the decision it reports: every parameter of `SaveTransferMath.CurtainHoldArmed` appears as a literal in `CurtainTakedownGate.State()`, the line still prints the answer (`holdArmed`), and it reads that answer off `SaveTransferCoordinator.CurtainHoldArmed` rather than recomputing one of its own | P13 P1 | incident | 2026-08-07 host+client capture: `17bf9fe` widened the arm from two inputs to three (adding `loadBoundaryAnnounced`, which IS that commit) and never touched `State()`, so nine `curtain lift PASSED gate unheld` lines across both logs print `holdArmed=` beside a parenthesis accounting for two of three terms. Not cosmetic and not general logging hygiene: on a lobby FIRST start and on the new-campaign arm the announcement is the ONLY term that can be true — `_begun` is false until `Begin()`, several yields past `ReadSavegameBinary`/`SendBlob`/`PrepareEntryFromBlobCrt`, and the latch is already spent by `ConcludeNewCampaignBootstrap` (that pair IS L86's report) — so the printed line at the exact boundary the newest fix exists for is `holdArmed=True (sessionStarted=False newCampaignPending=False)`: self-contradictory, and it left two full logs from both sides of one session unable to say whether the host's screen had been held. The swallow is INSIDE the instrument `State()` was built to end ("five successive fixes were written blind"), and it is regenerative — the arm has already grown 2→3 and every growth silently re-opens it. L86 was GREEN throughout: it asserts that the announcement is READ by the arm, never that the decision can be read back out of the log | POSITIVE CONTROL + premise-changed |
| L174 | one boundary, one load — FOR THE HOST TOO: the new-campaign bootstrap passes `hostHoldsThisWorld` so the host declines to re-enter an autosave of the world it is standing in (the flag still defaults FALSE, so the lobby start and the F2 reload — which load a DIFFERENT save — rebuild as before), and the skip still reaches the terminal state every other peer reaches: `SendLoadComplete` (no `Loaded→Playing` edge is coming, so nothing else would put slot 0 back in the done set), `ResetForReloadBoundary` (the one debt the skipped `PrepareEntryFromBlobCrt` owed), `BeginSuppressed(begun:true, hostEntryHold:false, barrierOpen:true)` still FALSE so SessionBegin still releases the clients, `AllDone` still refusing to hold on the host alone, and `EnterLevel` still self-guarding on `_begun` | P13 P1 | incident | 2026-08-07 host log, both loads dated: geoscape playable 23:09:04.174 (native campaign creation), then `BEGIN broadcast` 23:09:07.437 and a SECOND `OnReachedPlaying slot=0` 23:09:16.631 — 9.2 s deserializing bytes the host had produced from its own live campaign seconds earlier. `17bf9fe` removed the interactive geoscape that flashed BETWEEN the two and could not remove the load, which was never a curtain bug. It is also the whole of the companion report "the host finished loading first and could already play": literally true — his world was playable 16 s before the collective reveal, and that window exists only because a second load runs inside it. One structural defect, two complaints, and the reason the cosmetic half kept being fixed alone. NOT a new idea: `HostBeginTacticalEntryTransfer` has always made the same refusal ("the host does NOT re-enter from the blob"), so this removes an inconsistency between two boundaries. The same-bytes invariant is now paid for by DETECTION rather than assumption, and by reuse — each client CRCs one geoscape root per second on the host's own canonical walk (`GenericApplier.ClientCrcTick` → `DiffEngine.RootCrc`) and the host compares in `DiffEngine.HandleCrcReport`, "the ONE thing in the rail that ever compares host and client state"; no second fingerprint was built. Sibling of L122, which makes the same claim for a TACTICAL entry | POSITIVE CONTROL + premise-changed |
| L180 | a roster row whose JOIN never arrived is never HELD and never waits forever: `SessionManager.PausePeer` REMOVES a row carrying `Guid.Empty` instead of pausing it, `SessionLifecycle.UnjoinedRowExpired` is executed over its cube (a fresh unjoined row is not reaped, one past the deadline is, a HANDSHAKED row is never reaped at any age, and the always-false form is the positive control), `SessionManager.Update` actually consults it, the host's window outlasts the joiner's own `JoinStageTimeoutSec` so the peer always gives up first, and the whole law is guarded on the pre-JOIN row still existing (`NetworkEngine.OnPeerConnected` → `AddClient`). Its other half is the join path's silence: every stage, deadline expiry, cascade fallback, cancel and final abandonment now leaves a named `[Multiplayer][join]` line | P13 P1 | live bug | 2026-08-07 VPN join, BOTH logs: the friend's first join half-opened, he hung and was thrown to the main menu, and on the HOST he stayed — still in the lobby after he had quit the game and switched the VPN off. Host `23:07:08.540 P2P session … failed (P2PSessionError=4)` → `23:07:08.541 Peer 1 (Unknown) PAUSED … Roster row kept — it resumes its seat when it comes back` → `23:07:08.557 PEER_LIST fan-out MISSES 1 roster peer(s) [1]`; "Unknown" is `ClientInfo.PlayerName`'s initialiser, i.e. no JOIN had ever arrived. `94fb77b` closed the host's PACKET minting arm ("mints only on ConnectionRequest" — the `23:06:37.599 … minting nothing` line IS it working) and left the Steam P2P SESSION-REQUEST arm open, which must stay open: the host has to accept the session to receive a ConnectionRequest at all. So the row is minted by the transport connect with no identity, and `PausePeer` — right for the peer it was written for — preserved nothing and could never end (`ResumePeer` needs a heartbeat from a machine that is gone; the reaper only calls back into `PausePeer`, which returns on an already-paused row). The heartbeat reaper is not the deadline either: it keys on SILENCE and a half-open joiner is not silent (`RefreshLiveness` refreshes off any inbound byte) — the host carried that row 31 s and only Steam's own timeout ended it. CLIENT-side the whole 54 s was invisible: `MultiplayerUI.OnConnectionFailed`:1246-1307 contained no log call of any kind, so a fired deadline, a user cancel and a still-hanging attempt were byte-identical in the log. L84 was GREEN throughout — it asserts that funnel EXISTS, never whom it is for | premise-changed + POSITIVE CONTROL |
| L181 | both halves of the lobby start gate count the same peers, and the refusal NAMES them: `LobbyController.IsLivePeer` is the ONE definition (not host, not `Paused`, `PlayerGuid != Guid.Empty`) and is executed over all four shapes with a live joined peer as its own positive control; a roster whose only non-host row never joined must neither CLOSE the readiness fold nor report the gate OPEN (both directions asserted, each alone is a bug); `PeersBlockedBy` contains the blocking peer's nickname and is empty when that peer has readied; and `MultiplayerUI.RefreshGateFacts` reads `IsLivePeer` for the count half | P13 P11 | live bug | 2026-08-07: the gate itself is WANTED (owner ruling, `3161d33`, L84 arm (c) retargeted) — the defect is that its two clauses over one roster counted different sets. `AllLivePeersReady` skipped a PAUSED row as unreadyable while `RefreshGateFacts` took every non-host row, so L180's phantom satisfied "the host is not alone" while being excluded from "everyone has readied": a host could start a co-op session with a player who was not there. Un-pause the same row — a half-open joiner keeps its socket, so `Paused` is false — and the disagreement runs the other way: counted present, unable to ever press READY, PLAY and NEW CAMPAIGN dead with a message naming no culprit ("not every live peer has readied"). L84 arm (c) was GREEN through both and is REPAIRED rather than supplemented: its fixtures built every `PeerListEntry` with no `PlayerGuid`, i.e. exactly the pre-JOIN shape, so it proved the fold's behaviour only for rows no live peer has | premise-changed + POSITIVE CONTROL |
| L182 | a marker that was "shown" is actually SWITCHED ON, and a button that is not a button does not eat the map: `PingMarkers` reaches `GeoscapeGlobeMarkers.AddMarker` nowhere, every one of its methods calls `SetActive` at least as often as `Object.Instantiate`, its own ping still goes through `ShowLocal` (the only thing that answers "is this mine") and both showers tint (arm (d), `ready-button-face-not-built`, WITHDRAWN 2026-08-08 — see L220) | P4c P11 | incident | 2026-08-07/08: pings were AUDIBLE AND INVISIBLE. The cue is downstream of the marker (`Track` ends in `Cue()`), so the sound proved `AddMarker` had run and returned normally — and it had: only its POOLED branch calls `SetActive(true)` (GeoscapeGlobeMarkers.cs:72/:101), the fresh-instantiate branch (:62-66) never does, and `SitePointOfInterest` has no pooled instance when a ping lands. Same run: `ready button raycast NOT trimmed — its Button names no targetGraphic` + `OVERHANGS its HUD container ... drawn over the tactical map`, i.e. a ~250x40 invisible click-eater over the battlefield, which `TacticalView.IsCursorOverGUI()` turns into dead confirm clicks. L158 and L160 were both green throughout: nothing was blocked, no leaf written, nothing thrown, and no camera moved. **Arm (d) withdrawn 2026-08-08**: it required `TrimRaycast` to silence the clone and name a built face, and that requirement was itself the defect — the clone is an `Instantiate` of the native End Turn button, so its own graphics ARE its clickable face. The "~250x40 invisible click-eater" reading was wrong: the clone mirrors End Turn's `sizeDelta` every frame, so the rect it answers for is the art it draws, and a button answering the pointer over its own rect is a button, not a click-eater | premise-changed + POSITIVE CONTROL |
| L183 | a repaint observes the batch that LANDED the state, and its line says which of THREE things it saw: `GenericApplier.MissionLabel` is executed over all three (no mission / a mission whose def has not landed / the def name), `GenericApplier.SiteRootKeyOf` names the owning site for a write DEEPER than the site and refuses the site itself, a vehicle root, a character root, a global root and every degenerate path, `MarkOrderChange` actually consults it, `FlushOrderReseed` actually renders through `MissionLabel`, and the flush still calls the game's own `GeoSiteVisualsController.Refresh` | P11 P1 | live bug | 2026-08-07, BOTH logs: `23:09:52.626 structural create 'S#104.SerializationData.ActiveMission' applied (GeoCustomMission)` immediately followed by `23:09:52.626 [MP][site] repaint S#104 state=Functioning activeMission=none`, and four more identical lines across the session. Read straight it says the mirror never got the mission; it is not what happened. TWO lies, one per half. (1) the line rendered `s.ActiveMission?.MissionDef?.name ?? "none"`, and a descend create ships the TYPE only — every member rides a LATER packet by design — so a mission that exists, is constructed and has been handed to `GeoSite.RegisterMission`:1629 prints the same single word as a site with no mission at all. (2) `MarkOrderChange` decided the marker repaint from the TYPE of the entity written, and the batch that FILLS a mission writes a `GeoMission`, so it marked nothing and the create-time repaint stayed the client's last word. The host log closes it: the host answered the client's own backfill (`DiffEngine tick … changed=6` at 23:09:45.461 host clock, 362 ms after the create it sent), the client applied it, and nothing said so. `S#293` looked healthy only because an unrelated site-level leaf rode the same batch 9 ms later. Correction the logs also force: `GeoCustomMission` and `GeoAmbushMission` are plain `GeoMission`, and `RefreshMissionVisuals`:620 reads `ActiveMission as GeoUpdateableMission` — so no globe wrapper is drawn for either on ANY peer, and the "marker never draws" reading was the collapsed diagnostic talking | premise-changed + POSITIVE CONTROL |
| L184 | a resend request this peer DROPS leaves a line, and a scoped backfill is not a full resend: `GenericApplier.ResyncAllowed` is executed over all four corners (a scoped request survives a live full-resend window, a full one is still globally coalesced, the scoped window is per-root, and a pending backfill never blocks a full resend), `RequestResync` actually consults it and reaches `LogMissOnce` on the throttled branch, `LogMissOnce` routes through `RailMeta.CountMiss` so drops are countable, `ApplyEntry` reaches `NoteScopedAnswer` and `ClientCrcTick` reaches `ExpireScopedRequests` | P1 P7 | live bug | 2026-08-07: `RequestResync` had ONE global 5 s window with the warning INSIDE it, so a throttled request printed nothing whatsoever. For the unknown-kindId burst the coalescing was right and worked (39 warnings, 1 resend, converged in 1.3 s); the hazard was the COUPLING — the same window gated the SCOPED `structural create backfill` requests, which ask about one root's ref-lists and are not answered by a full resend that went out five seconds earlier for an unrelated reason. Second half is answer-absence: four scoped requests in that session, ZERO acknowledgement in EITHER log — the host never logged receiving one and the client has no positive line when the answer applies, and the host's answer is an ordinary delta by design (`DiffEngine.ForceReemit`), so "the resend never came" and "it came and applied silently" were byte-identical. The RCA for L183 had to be settled by reading `DiffEngine tick … changed=N` shapes on the host | premise-changed + POSITIVE CONTROL |
| L185 | a corpse's contents are answered by the ITEM, never by its place in the queue: `LootMirror` keys the host's manifest by item def guid and consumes one answer per question in whatever order the questions arrive, executed over the host's own order, the client's actual order, an interleaved one, a skipped entry, an unnamed item and an undeclared corpse | P1 P7 | live bug | 2026-08-07, BOTH logs, same death. `DieAbility.ShouldDestroyItem`:187-200 draws from `SharedData.Random` (a per-peer `new System.Random()`), so the host pre-rolls and every other peer answers from the manifest — and the manifest was a QUEUE scanned FORWARD to the matching def, on the stated premise that both peers ask in the same order "because it is the same method over the same inventory". FALSE: the host pre-rolls at `TacticalActorBase.Die` and enumerated [clip, clip, rifle] (host `Player.log` frame 47909 — `Will destroy item … AmmoClip` at 43 % and 40 % against a 75 % chance, then the rifle kept), while the client's own `DieAbility.DropItems` runs LATER in the same death, inside the die coroutine after `StopNavigation`, and asked [rifle, clip, clip]. Reaching the rifle discarded both clip answers and emptied the queue, so the client KEPT the two clips the host had destroyed — one tolerated line (`a corpse's contents arrived with no host manifest`, 23:17:51.587) and then, twelve minutes later, `item def 'a1345994-…' is in this peer's containers but in none of the host's` (23:20:08.172). `GetDroppableItems`:202-224 is inventory-then-addons over `GetComponentsInChildren` plus a `Distinct()` and promises no order at all. The `L67 loot-manifest-positional` arm was GREEN throughout — it asks about a SKIPPED entry, which a forward scan handles, and never about two orders of the same set | premise-changed |
| L186 | the weapon in a soldier's hands is the host's answer RE-ASSERTED, not an event that can be missed: the selection rides the 0x82 settle (`HostSettle` reads `EquipmentComponent.SelectedEquipment`, `ApplySettle` reaches `ReconcileSelection` INSIDE `SyncApplyScope`), the reconcile compares before it writes and marks the tactical UI dirty, and the game-side premise (`SelectedEquipment`, `SetSelectedEquipment(Equipment)`, `Equipment.ItemDef`, `EquipmentComponent.Items`) still holds | P1 P11 P7 | live bug | 2026-08-07, lost in BOTH directions inside one 15 s mission entry. INBOUND ×5 (client 23:14:09.582-09.885): `the host's weapon switch for actor 1..5 cannot be applied here — no tactical map on this peer to resolve actor key N against`. OUTBOUND ×3 on EACH peer (host 23:14:02.450-02.640 Soldier_6/7/8, client 23:14:23.937-24.076 Rancid/Cinder/Kain): `a weapon switch on X cannot be relayed — that actor has no shared key`, because `EquipmentComponent`:56 raises the enter-play selection before that peer has built its battle key map and `TacticalActorKey.Of` answers 0. Nothing retried either half, and the outbound line PREDICTED the `EquipmentNotSelected` refusals that follow — the shape that gets reported later as an ability bug. The selection rode only `OpSelectEquipment`, an event, which is exactly what a peer that is not ready yet loses permanently; L131 and L137 had already moved the status set and the trait set onto the settle for this same reason and the selection was left behind | premise-changed |
| L187 | a stale screen never undoes an applied intent, and the peer whose gesture is undone is told: `EquipSync.JudgeHostFlush` is executed over the whole cube (a wholesale write-back of the PRE-intent loadout is StaleRevert; an idle re-flush of what the host holds is Native; a real host edit RETIRES the memo; and the three positive controls — model already moved on, no-op intent, half-populated memo — decide nothing), `SetItemsCapturePatch.Prefix` actually consults `BlockStaleHostRevert`, the block reaches `ReseedLocalScreenAfterRemoteMutation`, `CheckLastApplyStuck` reaches `RejectAndNotify` and `HandleIntent` reaches `RecordApplied` | P3 P11 P1 | live bug | 2026-08-07 HOST log: seven consecutive client equip intents APPLIED then silently REVERTED — `nonce=43…49`, all `char=U#5`, inside eleven seconds, each undone within 150-1800 ms (`23:12:44.774 HOST intent APPLIED … nonce=43` → `23:12:44.918 HOST previous apply was REVERTED for char=U#5`). The client's half of that same window (nonces 43-52) carries NO revert, reject or correction line of any kind: it believed all ten gestures landed. `SetItemsApplyGate` blocks a view→model flush only INSIDE `SyncApplyScope`, and a host standing in its own equip screen while a remote intent lands is in no scope at all (`IntentRail.ShouldRunNative` returns true for a host) — so `UIStateEditSoldier.UpdateState` wrote its still-PRE-intent widget lists back over the applied loadout and the DiffEngine correctly emitted nothing, because a rail can only ship what the model holds. The existing `CheckLastApplyStuck` guard was GREEN through all seven: it DETECTS the same signature, one intent late, when the NEXT intent arrives — so `nonce=50-52` were never even checked. Repaired rather than supplemented — the detector keeps its job and gained the addressee it never had | premise-changed + POSITIVE CONTROL |
| L188 | a field the rail excludes on EVERY walk is reported as permanent, not as a warning: `DiffEngine.EscalateAt` is executed over its cube (the first sighting does not escalate, the threshold does, past it does not), the REAL per-line counter is driven for `threshold+5` walks with a second line advancing beside it (exactly one verdict, at exactly the threshold, and one line's count never spends another's), `ForgetIncidentCounts` really forgets, `Incident` routes through `NoteIncidentWalk` and reaches `Debug.LogError`, and `DiffEngine.Reset` reaches `ForgetIncidentCounts` | P1 | live bug | 2026-08-07 HOST log: `23:09:05.481 WARN [rail] DiffEngine excluded: FactionDiplomacy._factionsDiplomacyState: entity-list encode failed: polymorphic value PPFactionDef as IDiplomaticPartyKey` ×3 factions — at the FIRST campaign walk, and then never again for sixteen minutes while the field kept failing on every walk, ~2×/s. `Incident`'s `HashSet` dedups the LOG, never the RETRY, so a genuinely one-off exclusion during a load and a field that has NEVER crossed and never will produced byte-identical output: one Warning. The client's faction-relations screen ran on whatever the save transfer held at 23:09 and neither peer's log mentioned it again. Asserts the REPORTING deliberately — whether `PPFactionDef` can ride as an `IDiplomaticPartyKey` at all is an encode question owned by the codec (`RailMeta.EncodeValue`'s declared-type refusal + the frozen `polymorphic-codec: no` in `docs/rail-contract.txt`); this is what guarantees the NEXT field to die that way is not silent for sixteen minutes | premise-changed + POSITIVE CONTROL |
| L189 | a one-shot push the receiving peer cannot take is HELD, not lost: the real hold is driven (a peer that dropped nothing reports none; a dropped list is kept; `ClearHeldOffers` and `Reset` both retire it), the drain's own IL order carries no `ClearHeldOffers` after its `ApplyOffers` (a replay that fails is still held), `HandleInbound` reaches `HoldOffers`, `ApplyOffers` reaches `ClearHeldOffers`, the drain reaches `ApplyOffers` + `SurfaceSeq.ShouldApply` + `Mark`, and it targets `GeoMarketplace.OnLevelStart` — which still builds `MarketplaceChoices` | P1 P11 | live bug | 2026-08-07, BOTH logs: `HOST 23:09:06.209 shipped 14 offer(s) seq=1 (reroll)` → `CLIENT 23:09:13.725 offer list DROPPED — this peer has no live marketplace …; the next push converges it`. `seq=1` is the ONLY market push in the whole sixteen-minute session, so the drop line's own guarantee is FALSIFIED BY THE LOG — and it was not a race that could have gone the other way: host+7.5 s = 23:09:13.7 against the client's own `SendPrepared` for that same first campaign load at 23:09:15.484, i.e. the client was GUARANTEED mid-load at the only moment offers are ever sent, and its shop stayed 14 offers behind. The SHAPE, not the field: a push happens on a reroll, a purchase or a reject reconverge (all HOST events) while the drop condition is RECEIVER state the host cannot see and does not poll. The answer is the one already in this codebase — `EventPopup._held` + `DrainHeldRaises`, whose three HELD raises that same session all landed. STILL MISSING IT, named so the next one is not rediscovered from a log: `GeoModalMirror`:591 and `CutsceneMirror`:201 | premise-changed + POSITIVE CONTROL |
| L193 | the harness cannot report a verdict it did not earn: `Program.Add` CONTAINS each law's exception (a catch clause specifically — `foreach` always emits a try/finally, so counting handlers passes with the catch deleted), `Run` reaches `AbortOnHarnessCrash` so a HARNESS-CRASH line can never be baselined, `Run` reaches `StaleBuild` so no verdict is issued about a `Multiplayer.dll` older than `src/**/*.cs`, and `Run` calls `List<string>.AddRange` NOWHERE — checked with `CalleeSequence`, not `Callees`, because mscorlib is outside the one-assembly filter | P1 | live bug | 2026-08-07/08, the only law whose subject is the RUNNER: every other law is worth exactly what this one is true, because a red law gets argued with and a GREEN harness gets believed. TWO unearned GREENs in one session. (1) GREEN BY OMISSION — `laws.AddRange(X.Check())` let a throwing law abort the run: `RailMeta.CountMiss` reading `UnityEngine.Time.realtimeSinceStartup` (an ECall that throws outside the player) died at law #31 of 153 and the 122 after it reported nothing, and `L98_ApAuthority` `Invoke`s a production method by a hardcoded 7-argument array, so another agent changing that signature took the net down for six agents as a `TargetParameterCountException`. A law that Invokes by a hardcoded argument array IS a tripwire on that method's signature and fires as a crash rather than a red line. (2) A VERDICT ABOUT A DLL NOBODY BUILT — `dotnet run --no-build` reflects over a stale assembly: one run produced thirteen violations that were pure artefact, and worse, an agent falsifying a new law got GREEN with the asserted call DELETED and nearly rewrote the law to match. `.githooks/pre-commit` was checked and has no equivalent hole (plain `dotnet run`, ProjectReference rebuilds the mod). Arms (a) and (d) were themselves VACUOUS on first write and only falsification found it | premise-changed + POSITIVE CONTROL |
| L192 | a def under an interface-declared slot rides as the LEAF it already is, and nothing wider does: encoding a `PPFactionDef` through an `IDiplomaticPartyKey`-declared `EntityList` slot no longer throws, the decoder KNOWS the tag that encode writes (an unknown one dies as `bad blob tag` in `DecodeValue`'s default, BEFORE any def lookup — the only half decidable in a console host, since the DefRef codec ends in a `DefRepository` ECall), and the POSITIVE CONTROL is the narrowness itself: a plain derived class under a plain base class must STILL throw `NotSupportedException` | P1 P2 | live bug | 2026-08-07 HOST log ×3 factions at the first campaign walk: `DiffEngine excluded: FactionDiplomacy._factionsDiplomacyState: entity-list encode failed: polymorphic value PPFactionDef as IDiplomaticPartyKey` — the whole field aborted (an EntityList aborts WHOLE when one element will not encode) and faction diplomacy never crossed to a client again for the remaining sixteen minutes; L188 is the REPORTING half of the same incident, this is the encode. NOT object polymorphism: a `BaseDef` is already a `LeafKind.DefRef` leaf whose entire wire form is a guid the client resolves from its OWN repository, and it reached the blob path only because the DECLARED slot is an interface the leaf test cannot see through. The slot that actually fires is `Relation`'s `[SerializeCustomCreate]` `WithParty` create param (`EncodeObjectBody`'s `ci.Params` loop), reached from `_factionsDiplomacyState` → `FactionDiplomacyState.Relation`. Does NOT fix, and does not hide, the three CLASSIFY-time exclusions beside it — `FactionDiplomacy._relations` (`docs/rail-baseline.txt`:488), `FactionDiplomacy.Party` (:486), `Relation._thisParty`/`_withParty` (:132-133) — a reviewed, visible, never-silent class; what changes is that the FIELD they sit in now ships at all. The arm is gated on the RUNTIME value being a `BaseDef` and nothing else, so `docs/rail-contract.txt`'s frozen `polymorphic-codec: no` does NOT flip and the reviewed closure is unchanged (`types=96` before and after) — VERIFIED by falsification: deleting the declared-type refusal instead produced `RAILCHECK RED (contract drift — architectural promise changed)`, i.e. the alarm works and this change does not trip it, so **L55's `polymorphic-codec: no` premise stands** | premise-changed + POSITIVE CONTROL |
| L194 | with the host CURTAINED, a host-local raise is queued only after its own reveal: `EventPopup.HostRaiseWaitsForReveal(inSession,isHost,revealed)` executed on all four corners, `EventRaiseBroadcast.Prefix` reaches `HoldHostRaiseBehindCurtain`, `DrainHeldRaises` reaches `DrainHostCurtained` (it used to walk away on `IsHost` and `_held.Clear()`), the replay re-invokes the GAME's `OnGeoscapeEventRaised` instead of calling `QueryStateSwitch` itself, and the Prefix/Postfix pair shares a `__state` so a PARKED raise is not mirrored early | P13 P1 | live bug | 2026-08-08 campaign start, 3 peers: HOST got `IntroBetterGeo_0/1/2` then the intro cutscene, both CLIENTS got the cutscene then the three dialogs. All four view requests are priority 0, so `GeoscapeViewSwitchQuery` serves them FIFO and INSERT ORDER was the only difference: a client cannot carry a window at the loading screen so its raises sat in `_held` and were replayed after `RevealAll`, while the host's geoscape is live behind its own curtain and `GeoscapeView.Update` keeps calling `ProcessQueriedStateSwitch` throughout it — the curtain hides the screen, not the queue. Nothing anywhere held a host-LOCAL raise; L189 covers only the receiving peer's half. Falsified RED by restoring `\|\| engine.IsHost` → `host-exempt-from-its-own-hold` | premise-changed + POSITIVE CONTROL |
| L195 | a mirrored window is not released on top of a FULL-SCREEN ONE-SHOT: `EventPopup.OneShotHoldsWindow` is true for `UIStateGeoCutscene` (by assignability) and FALSE for the map, an ordinary window and no state at all, `CanCarryWindow` consults it, and `DrainHeldRaises` asks the gate BEFORE it pops the entry (HELD, not lost) | P1 | live bug | 2026-08-08: the intro cutscene started and all three `IntroBetterGeo` dialogs were released on top of it within 0-130 ms, because `CanCarryWindow` asked only whether the event system had finished loading (its 2026-08-04 subject) and knew nothing about what is on screen. Compounded by the stale input override — `UIStateGeoCutscene.EnterState`'s `SetInputState("Cutscene")` is stored and discarded while one is held (`RevealInputLock`:19-23, `InputController.SetInputSets`:520-527) — so Escape was dead and the peer sat through the whole video with three modals behind it. L196 owns that latch. Falsified RED by deleting the term from `CanCarryWindow` → `gate-ignores-the-one-shot` | premise-changed |
| L196 | the reveal's input unlock lands BEFORE this peer releases any queued window: `RepairRevealInputLock` is callee **#0** of `SaveTransferCoordinator.Update`, `NetworkEngine.Update` runs the coordinator BEFORE `SyncEngine.Tick`, the repair is not role-gated, and the tick it is ordered against really does reach `EventPopup.DrainHeldRaises` | P13 P1 | live bug | 2026-08-08, the prediction L175's own text made ("holding a cutscene behind a player with nothing able to release it") arriving inverted: the cutscene was ENTERED, and its `SetInputState` was discarded because a loading override was still held that frame. L142 asserts the DECISION and its wiring and was GREEN throughout — correctly, the invariant WAS converged, just not before the frame a queued state entered. This is an ORDERING law and the only ordering the mod controls: input is unlatched before any window it holds is released. Falsified RED by moving `RepairRevealInputLock()` below `FlushLoadComplete()` → `unlock-is-not-first` | premise-changed |
| L197 | a peer that did NOT answer the mission event still reaches `UIStateRosterDeployment`: `MissionArrivalNav.MissionHasArrived` / `ArrivalGivenUp` executed on every corner, `SyncEngine.Tick` reaches `MissionArrivalNav.Tick`, BOTH raise paths (`HostBroadcast` and `RaiseMirrored`) arm `Watch`, `Step` reaches `GeoscapeView.LaunchMission` and REUSES `ShouldOpenDeployment` + `AlreadyHeadedForDeployment` (no double-queue into a list that is part of the SAVE), the two pure decisions read no peer type, and `MissionEncounterNav.Postfix` carries ≥7 explicit outcomes | P13 P11 P1 | live bug | 2026-08-07: on the peer that did not answer, `MissionEncounterNav.Postfix` emitted NO LINE AT ALL — not the success (:438), not the warning (:431), not the catch (:446) — so it bailed at one of FOUR UNLOGGED returns (:411/:417/:420/:423) and which one was unknowable. The funnel is a LOCAL DIALOG event that fires only if this peer's own window was built, clicked through and torn down, and reads whatever that window holds at that instant — on a mirroring peer a `Record` that can still be the placeholder `RaiseMirrored`:568 minted. Trigger moved to state that arrives on every peer by itself (the record resolving + the `S#…ActiveMission` structural create); armed only from raises this peer was SHOWN, so the missions the geoscape sprouts all game long can never yank anybody. NOT the deployment countdown (`DeployCountdown`'s only entry is `MissionSync.cs:143`, reachable only from a Deploy press) and NOT the outcome page (`EventPopup.cs:728-733`, by construction). Falsified RED by unwiring the tick → `arrival-drives-nothing` | premise-changed + POSITIVE CONTROL |
| L198 | the countdown's CANCEL is a button a click can actually reach, and no step of the veto bails without a line: `CountdownPanel.Attach` calls `EnsureRaycaster` (which names `GraphicRaycaster` on both a `GetComponent` and an `AddComponent`) and `SkinButton` (which assigns `Selectable.targetGraphic`), the button is wired to `OnCancelClicked` rather than straight to `RequestCancel`, all three of `OnCancelClicked`/`DeployCountdown.RequestCancel`/`DeployCountdown.Cancel` contain a `Debug.Log*`, and `MultiplayerUI.Awake`/`Update` really do reach `Attach`/`Sync` | P13 P11 P1 | live bug | 2026-08-08, the countdown's FIRST live exposure, 3 peers: the owner pressed CANCEL and the drop went ahead — and all three mod logs were EMPTY on the subject (host had `launch HELD for 5 s` 01:51:59.118 and `countdown reached zero` 01:52:04.173, both clients had the mirrored `DeployCountdownState` arriving, nobody had a cancel line). The click never reached a handler AT ALL: `MultiplayerUI.EnsureBarCanvas` built the mod's overlay root as `Canvas` + `CanvasScaler` and NO `GraphicRaycaster`, so every widget on it was decorative by construction. Unnoticed for as long as it was because nothing on that canvas had ever been clickable — the in-game bar and `PlayerPanel` set `raycastTarget = false` on every Graphic deliberately, and this Cancel is the first interactive thing to land there. The game's own from-code canvas is all three components together (`LlockhamIndustries.Misc/SceneSingleton.cs:81-83`). Second half: a `Button` added at RUNTIME has a null `targetGraphic` (Unity fills it only from the editor's `Reset`) — the identical omission L182 repaired on the cloned End Turn button (`TacticalReadySync.cs:522`), and the game sets it beside the Image (`SnapshotTextPooling.cs:145-148`). L177 was GREEN throughout and rightly so: it asserts the countdown's DECISION and has no opinion about the surface the veto is pressed on — the L175/L191 shape again. Falsified RED by deleting the `EnsureRaycaster(barCanvas)` line -> `canvas-eats-every-click` | premise-changed + POSITIVE CONTROL |
| L199 | the countdown caption is sized by a layout component and cannot leave its plate: `CountdownPanel.FitInsideRect` writes all five of `resizeTextForBestFit`/`resizeTextMinSize`/`resizeTextMaxSize`/`horizontalOverflow`/`verticalOverflow`, `Attach` applies it to the title and `SkinButton` to every button label, and both are applied AFTER `UiToolkit.CreateText`/`CreateButton` built them (asserted in IL order) | P11 | live bug | 2026-08-08, same session: "MISSION STARTS IN 5 SECONDS" drew straight through both edges of the panel background. Not a styling slip but a DEFAULT — `UiToolkit.CreateText` leaves `horizontalOverflow = Overflow` on every label it builds, right for the lobby's generous rows and wrong for a 260-wide plate carrying a header-size sentence. Fixed with uGUI's own auto-size rather than a font size or an offset (either would be a number that happens to work at one resolution, in English, at one UI scale — and the panel is otherwise fully derived, `PanelH = Pad*3 + TitleH + ButtonH`): the frame stays fixed and the TEXT adapts, using the SAME five writes the mod already puts on the native End Turn caption (`TacticalReadySync.FitLabel:625-635`), the owner's stated reference for a correct button. `TitleH` went `Scale(26)` -> `Scale(44)` so the wrap has two lines to use instead of best-fit shrinking to unreadable. The ORDER arm is the non-obvious half: `CreateText` assigns the overflow modes itself, so a fit applied first is a no-op that reads exactly like a fix. Falsified RED by deleting the `horizontalOverflow` write -> `label-can-overflow` | premise-changed + POSITIVE CONTROL |

| L210 | a CLIENT-initiated FREE-AIM shot actually EXECUTES: the host asks the game's gate the way the game's own confirm asks it — `TacticalCommandSync.HostGateFilter` hands a `ShootAbility` (and only a `ShootAbility`) `IgnoredAbilityDisabledStatesFilter.IgnoreNoValidTargetsFilter`, exactly as `UIStateShoot.ConfirmShoot`:1295 and `UIStateFreeCam.ConfirmShoot`:466 do, waiving that ONE state and no other, and the filter is passed INTO `GetDisabledState` rather than subtracted from its answer | P5 P3 | live bug | 2026-08-08 owner: on a client, aimed / free-aim (FPS) shooting did nothing at all — the soldier simply never fired; movement was fine. The intent DID leave the client and DID reach the host; the host refused it with `NoValidTarget` ("Нет подходящей цели") five times across two peers and four soldiers, 01:53:12–01:54:19. Target choice was IRRELEVANT — friendly fire is legal, and the HOST'S OWN free-aim shot at an ally 7 s later logged the SAME disabled state at its confirm and fired for 76 damage, because a host click takes the `RelayMirror` branch and never reaches `Validate`. Root: `Validate`'s `abilityDisabledReason` arm ran the UNFILTERED `GetDisabledState()`, whose `NoValidTarget` arm (`TacticalAbility.GetDisabledStateInternal`:464-466) is for a shoot `ShootAbility.HasValidTargets`:41 = `GetTargets().Any()` — the SAME grid floor-cast sweep L169 had just established a free cursor point is not drawn from. So L169 pulled the sweep out of one gate and left it deciding in the other. **L169 was GREEN while this reproduced and could not have caught it**: its arm (a) passes `Validate` a `null` disabled reason, stubbing out the very arm that refuses, and its stub ("published a list that matched nothing") is self-contradictory with the live case — a sweep that publishes anything makes `HasValidTargets` TRUE and never reaches `NoValidTarget`. Cannot widen the choice gate: `NoValidTarget` holds iff the sweep is EMPTY, which is precisely `ChoiceIsOffered`'s `abilityPublishedAnyTarget == false` ("no list to be held to"), already accepted | premise-changed + POSITIVE CONTROL |

| L220 | the tactical READY CONTROL is reachable and a press leaves a mark: every method of `TacticalReadyButton` calls `GameObject.set_layer` at least as often as it constructs a `GameObject` (counted — the class builds TWO children), `TacticalReadySync.Toggle` reaches `Debug.Log` on the path that flips the flag, `TacticalReadyRowFollower` puts the reachability question to `EventSystem.RaycastAll` at the button's own centre and can reach `Debug.LogError` with the answer, and NO method of `TacticalReadyButton` sweeps the clone with `GetComponentsInChildren<Graphic>` while clearing `raycastTarget` | P4c P13 | live bug | 2026-08-08 owner: the co-op ready indicator next to End Turn took NEITHER hover NOR click, on the host and both clients. Not a rail defect and not a wiring defect — every wire was correct. The clone comes from `Object.Instantiate`, which carried the prefab's UI layer across; its two hand-built children (`MP_ReadyGreen`, `MP_ReadyHit`) came from `new GameObject`, which starts on layer 0 (Default) where the HUD camera does not draw. An undrawn graphic keeps `CanvasRenderer.absoluteDepth == -1`, and uGUI opens its raycast with `if (graphic.depth == -1 \|\| !graphic.raycastTarget \|\| graphic.canvasRenderer.cull) continue;` ("-1 means it hasn't been processed by the canvas, which means it isn't actually drawn" — `GraphicRaycaster.Raycast`, 2019.4). Because `TrimRaycast` had by then silenced all twelve NATIVE surfaces in favour of that one built face, the button's entire hit footprint was a graphic nothing could hit: no `OnPointerEnter` (hence no hover frame — the highlight is driven from `PhoenixGeneralButton.OnPointerEnter`:214-227, not a colour tint) and no `OnPointerClick` (hence no `Button.onClick`). Fix is the one line Unity's own runtime UI factory takes (`DefaultControls.CreateUIObject` = `go.layer = parent.layer`). **L182 arm (d) was GREEN throughout and could not have caught it**: it asserts `TrimRaycast` CALLS `set_raycastTarget` and `set_targetGraphic`, and it did call both — the call happened, the outcome was dead. The shipped diagnostic was green-while-broken for the same reason: it counted raycast surfaces ("remaining: 1 [MP_ReadyHit]") without ever asking whether one was drawn. **2026-08-08 round 2 — the layer was NOT the cause.** The layer fix shipped and the button stayed dead on all three peers. The new diagnostic settled it: `MP_ReadyHit(layer=5 depth=258)` — drawn, batched, `raycastTarget` true, the sole surface — and the `depth == -1` LogError never fired and no press line exists in any of the six logs. So the layer line is correct hygiene (it is what `DefaultControls.CreateUIObject` does) but it repaired a gate that was never the one closing. The REGRESSION is `30a64e3`: before it `TrimRaycast` bailed out ("its Button names no targetGraphic") and left all twelve NATIVE graphics live, and the button worked — proven by the owner reporting it ATE MAP CLICKS, which only a hit-testable widget can do. `30a64e3` silenced those twelve and substituted one hand-built face, and it died. Fix: delete `TrimRaycast` — the clone is an `Instantiate` of the native End Turn button and its own graphics are its face, exactly as End Turn's are. **Two laws MANDATED the defect** (L127 arm (d), L182 arm (d)); both withdrawn, and deleting the trim is what turned them red. **This law's own arm (c) was the fifth assert-the-call law**: it required the diagnostic to READ `Graphic.depth`, which it did — over a dead button — because every field it inspected was one we set ourselves. Replaced by a probe that can come back NO: only the raycaster the game actually uses applies the ancestor `ICanvasRaycastFilter` chain, the canvas a graphic is registered against, and whatever is drawn on top. Still asserts NO gate: nothing waits on the tally (L119 keeps that) | premise-changed + POSITIVE CONTROL |
| L230 | every peer starts an attack animation from the HOST'S RECORD, including the peer that clicked, and no actor waits for an echo forever: `TacticalCommandSync.OrderWaitsForTheEcho` defers every rider that is not an `IMoveAbility` (run to exhaustion over all eight inputs), the click seam `ClickedOrderWaitsForTheEcho.Seam` really resolves to `TacticalViewState.ActivateAbility` with its exact four parameter types, `EchoCeilingFrames` is positive AND longer than the host's own `DeferCeilingSeconds` hold with `TickEchoWaits` reaching `Debug.LogError`, `PublishClickedOrder` hands the HOST's own click to `HandleActivate`, and `OnAbilityActivated` decides the mirror's exclude argument through the same one rule | P1 P5 P13 | live bug | 2026-08-08 owner: "I throw a grenade and on one instance it has already exploded while on another it is only just leaving the hand; one player is still aiming while on another that soldier has already fired and the enemy is dead". Nothing was dropped — the SAME order simply had three start times by construction: the acting peer began at its own click (A3a's speculative local play, `TacticalCommandSync.cs:1254`), the host when the intent landed (+&half;RTT), the watchers when the mirror landed (+&half;RTT again). Every peer was correct in isolation, which is why no existing law could see it: an assertion that the intent is SENT was green throughout. Owner's ruling: the speculation is retired — the acting peer plays nothing and starts from the host's mirror like everybody else, paying its own ping. The HOST is half the defect and is not exempt: its click reached `Activate` natively, took the `RelayMirror` branch and never touched `Validate` at all (the asymmetry L210 measured, 76 damage on the state that refused five client shots), so its own click is now serialised to the wire format and fed to `HandleActivate` with peer 0. Seam is the game's ONE click funnel `TacticalViewState.ActivateAbility`:259 (`UIStateShoot`:1385 is its only override and it calls base), NOT `TacticalAbility.Activate` — that one is VIRTUAL and skipping its base body still lets `ShootAbility.Activate`:165-174 run its own `PlayAction(Shoot)`. One exclusion, by the game's own marker interface `IMoveAbility` and not a concrete class: `FollowupAbility` is in `TacAbilityTargetCodec.Dropped`, so deferring a move would DELETE the acting player's follow-up attack rather than delay it. NOT a quorum — the only peer waited on is the host, which answers by itself, and the wait is bounded at 12 s and released with `ECHO LOST`. Falsified RED on all five arms in turn (inverted rule; widened `Seam` param; ceiling below the host hold; `LogError`->`LogWarning`; host branch calling `ability.Activate`; `exclude` back to `_replayOriginPeer`) | premise-changed + POSITIVE CONTROL |
| L231 | a replicated action runs its WHOLE local animation before its outcome lands, and no peer is left aiming with no way out: `TacticalAimPoseSync.StanceMustWait` withholds the stance write while the engine is executing an ability on that actor (run to exhaustion over all four inputs), `ApplyStance` decides through it AND through the engine's own `HasExecutingAbility`, a withheld write is PENDED onto the `HashSet<int>` and retried (`ApplyStance` → `_pending.Add`, `RetryPending` → `ApplyStance`, `AimStatePatch.Postfix` → `RetryPending`), and both `TacticalCommandSync.ApplyActivate` (refusal branches included) and `TacticalCommandSync.TickEchoWaits` reach `ReleaseLocalUiHolding` | P1 P5 P13 | live bug | 2026-08-08 owner: "my soldier had finished shooting and the enemy died about two seconds later, and then that peer got stuck in aiming mode and no button did anything". L230 had just aligned every peer's animation START; the ENDS then disagreed because the HOST's own playback stalled. Measured across three logs of one shot: acting peer `FireWeaponAtTargetCrt() finished` 309.068, damage 310.419, death 310.553 — 1.5 s after the shooter visibly stopped; host `wait while [AIM_START_ANIMATION_IS_ACTIVE]` 536.391, `HOST aim-pose 3 -> <none>` 536.408 (17 ms later), `Actor Soldier_5 has timed out waiting for aim animation in the fire coroutine. Current animation is HL_IdleAlert_AR` 540.043; the third peer, which never touched that soldier, activation→damage 0.679 s against the host's own unstalled 0.650 s. CAUSE: a collision between two of our own surfaces. `ReleaseLocalUiHolding` takes the acting peer out of `UIStateShoot` the instant its mirror lands, `Emit` correctly announces a stance that peer no longer holds, and that clear reaches the host one frame INTO the shot, where `ApplyStance` called `PathProcessorUtils.SetNullNavParams` on the very animator `TacticalLevelController.FireWeaponAtTargetCrt`:1646-1665 had just written with `SetAimParams(AimStart)` and was spinning on — the checkpoint never cleared, so the shot sat out its whole five-second (actor-timed) timeout on the ONE peer that decides when damage is dealt. Nothing was dropped and no message was late, which is why "the stance is relayed" stayed green through all of it. The stuck-aim half is the same root: since L230 the click's native activation is suppressed WITH its state switch, so `ReleaseLocalUiHolding` is the only exit from a targeting state, and `ApplyActivate`'s three refusal returns plus `TickEchoWaits`'s ECHO-LOST release had none. NOT a quorum: the wait is on the LOCAL engine finishing an animation it is already playing, which ends by itself, and the retry guarantees it ends at all | premise-changed + POSITIVE CONTROL |
| L232 | a peer that suppressed its native state switch ALWAYS ends up out of the targeting state — on the host and on a client, on ACCEPT and on REFUSAL: `TacticalCommandSync.SuppressedClickIsStranded` executed over all 32 rows (host / mirror-back / settle-only / ceiling-expired all release; the ONE stranded row is a client still in flight inside the ceiling, which is the non-vacuity control), `PublishClickedOrder` + `ApplyActivate` + `ApplySettle` + `TickEchoWaits` all reach `ReleaseLocalUiHolding`, the echo disarm (`NoteEchoArrived`) sits in `ApplySettle` and NOT in `QueueSettle`, and exactly ONE patch class in the mod binds `TacticalViewState.ActivateAbility`, and `TargetingCameraHints` still names BOTH `Aim` and `ManualAim` with `ReleaseLocalUiHolding` reaching `CameraDirector.RemoveHint` | P1 P2 P13 | live bug | 2026-08-08, a week of the same report: "a peer gets stuck in aiming and no button does anything". L139 asserted the MIRROR releases and was green throughout, because two OTHER answers released nothing. (1) REFUSED ORDER ON A CLIENT — the host rejects and sends only `HostSettle(forced: true)`, never a 0x82 mirror; `QueueSettle` cleared `_awaitingEcho` at ARRIVAL, deleting the 12 s ECHO-LOST release, while `ApplySettle`'s own release hung off `actor.HasExecutingAbility()` — a guard written when the acting peer still played speculatively, which A9/L230 retired, so that actor is IDLE in exactly the case needing release. Wait cleared, no mirror, no release, no exit for the rest of the battle. (2) THE HOST HAD NO EXIT AT ALL, accepted orders included: `HandleInbound` and `ClientTick` both return early for the host, so `ApplyActivate`/`TickEchoWaits`/`ApplySettle` are client paths, `HandleActivate`'s four exits carried no release, and `_awaitingEcho` is never armed on the host, making its one remaining call site dead code there. Also folded in: `BusyActorBelongsToThePeerThatStartedIt` was a SECOND unprioritised Harmony prefix on the same `ActivateAbility` as `ClickedOrderWaitsForTheEcho` — Harmony stops at the first prefix returning false and the order between unprioritised patch classes is undeclared, so on any load one of them silently did not run; deleted, and `RefuseLocalCommand` now runs at the top of `PublishClickedOrder`, sharing the one release seam. (3) THE AIM CAMERA, same swallow one link further out: the suppressed `SwitchToState` carried a CALLBACK as well as a state — `TacticalViewState.ActivateAbility`:273-275 builds the `UIStateWaiting` whose `UpdateState`:32 invokes `UIStateShoot.ShootAbilityFinishedExecutionHandler`:1330/:1350, the ONLY pop of `CameraDirectorHint.Aim` once the click set `_abilityActivated`:1385. `ExitState`:1263-1270 guards its own pop with `&& !_abilityActivated`, so `ResetViewState` does not do it, and it never self-heals: `CameraDirector.Evaluate`:163 sweeps only `Unmanaged` params and `TacOrbitCamDirectorParams` never sets it (only `Weapon`:459-465 `ProjectileFired` and `TacticalView`:911 `ActorReveal` do, whole-assembly), so every later evaluation — including the ones another peer's plain moves cause — re-matches the orbit node. Clients sat in the shoulder camera for the rest of the battle. The release now runs the states' own `RemoveHint` pair, before its already-neutral early return. NOT a quorum: every release here is this peer's own answer arriving or its own clock expiring, and no peer waits on another peer ACTING | premise-changed + POSITIVE CONTROL |
| L240 | the ability the rail RESOLVES is the SELECTED weapon's, at BOTH ends: `CandidateMatchesSelection` executed over two distinct sources in all four polarities (selected wins, unselected loses, a def mismatch loses, a sourceless candidate never wins by `null == null`), `HandleActivate` AND `ApplyActivate` both resolve through `TacticalCommandSync.ResolveAbility` and neither calls `GetAbilityFiltered` itself, and the resolver really reads `EquipmentComponent.SelectedEquipment` and really uses the matcher (closures scanned) | P1 P2 | live bug | 2026-08-08, one soldier with broken arms. `Overwatch_AbilityDef` and `Reload_AbilityDef` mint ONE INSTANCE PER WEAPON (`OverwatchAbility.OverwatchWeapon => GetSource<Weapon>()`; `ReloadAbility`'s source is the `TacticalItem`) and `ActorComponent.GetAbilityFiltered`:211-221 returns the FIRST match, so the host always answered for the PRIMARY. Client 3: `02:45:42.816 select -> PX_Pistol` ACCEPTED seq=259 -> `02:45:43.965 command Reload_AbilityDef` -> `.970 CLIENT weapon switch applied -> PX_SniperRifle` -> `.970 reject ... Недостаточно свободных рук`. Five reloads refused; the sixth accepted at 02:46:18 only because an inventory move had removed the rifle. Overwatch was offered and refused for that same rifle by the same mechanism. The game's own disambiguator is the SELECTION (`TacticalAbility.Activate`:1087-1090), so resolving by it relaxes nothing. Falsified RED on all four arms (matcher ignoring `selected`; each site reverted to first-match-by-guid; resolver stopped reading `SelectedEquipment`) | premise-changed + POSITIVE CONTROL |
| L241 | a RELOAD lands on the weapon in his hands and a broken arm still refuses the rifle: the shipped `CandidateMatchesSelection` walked over slot-ordered candidates picks the SELECTED pistol and the shipped `Validate` then returns null (ACCEPTED), while a `NotEnoughHands` answer still produces a refusal carrying that literal; `HandleActivate` resolves before it asks `GetDisabledState` and never calls `GetAbilityFiltered`; game-side premise (`AbilityDisabledState.NotEnoughHands`, `Equipment.HasEnoughHandsToUse`, `ReloadAbility : TacticalAbility` and NOT `ShootAbility`, `CommonItemData.CurrentCharges` + `TacticalAbility.GetRequiredCharges`) | P1 P2 | live bug | 2026-08-08: pressing RELOAD on the pistol made the soldier SWAP to the primary rifle and then fail — he could never reload at all. Five refusals, each `Недостаточно свободных рук` = `NotEnoughHands` (`GetDisabledStateInternal`:419 over `Equipment.HasEnoughHandsToUse`). Never a hands rule and never a mod lock: the gate was asked about the RIFLE while he held the PISTOL (L240). This law asserts the OUTCOME so the fix cannot be simplified away, and its second half exists so the symptom is never made to disappear by relaxing the game's rule. Falsified RED (matcher ignoring `selected` -> `reload-refused-for-the-wrong-weapon`; site reverted -> `gate-asked-before-resolving`; `Validate` swallowing `abilityDisabledReason` -> `broken-arms-were-relaxed-away`) | premise-changed + POSITIVE CONTROL |
| L242 | an order the host REFUSED spends no per-turn use: `HostUsesFor` executed — an ABSENT wire entry means ZERO, a null dictionary means ZERO, a present entry means its own number; `HostSettle` collects the counts, `PendingSettle.Uses` carries them, `ApplySettle` reconciles them, the reconcile walks the ACTOR'S abilities (not the wire's keys), drives them through `Reset`+`Increment`AbilityUsesThisTurn and marks the tactical UI dirty; game-side premise that `TacActorInstanceData.AbilityUsesThisTurn` is still keyed by `TacticalAbilityDef` | P1 P11 | live bug | 2026-08-08: after switching weapon to the pistol, overwatch was refused with "cannot be used again this turn" on a soldier who had never overwatched. The GAME'S own rule, not a mod lock — `TacticalAbility.Activate`:1092 `IncrementUsesThisTurn`, counter `TacticalActor._abilityUsesThisTurn`:113 keyed by DEF (one entry for every weapon's copy), cleared only at the turn edge (:1194), `UsesPerTurn` defaults to 1. The client's speculative activate spent it; the host refused BEFORE activating so its count stayed 0, and a weapon change cannot clear a def-keyed counter by design. The mod replicated it NOWHERE (zero references in `src/`). L145–L147 are NOT implicated. Carried on the settle rather than reset at the reject, because the settle also sweeps every keyed live actor at every turn edge and so repairs the counter whatever leaked it. Falsified RED on five arms (absent -> -1; `CollectAbilityUses` dropped from `HostSettle`; `ReconcileAbilityUses` dropped from `ApplySettle`; `Reset` dropped so only raises are possible; loop over `host.Keys`; `MarkDirty` dropped) | premise-changed + POSITIVE CONTROL |
| L243 | a REFUSED order does not move the acting peer's selection: `SelectionMoves` executed to exhaustion — it does NOT fire for the weapon already selected, nor for a sourceless ability, nor for a `UsableOnNonSelectedEquipment` def, and it STILL fires for a genuinely unselected source (the belt is not withdrawn); `PreSelectSourceEquipment` routes through that decision, and `HandleActivate` RESOLVES before it pre-selects | P1 P11 | live bug | 2026-08-08. L186 was GREEN WHILE BROKEN: it asserts the selection PROPAGATES, which it did flawlessly — including propagating a pre-select the acting peer never made. `02:45:42.816 select -> PX_Pistol` ACCEPTED, then `02:45:43.965 command Reload_AbilityDef`, then `.970 CLIENT weapon switch applied -> PX_SniperRifle`, then the refusal. L186 is NOT withdrawn: its requirement is right and still needed, it was merely incomplete, and this is the missing half — nothing asserted the published selection was the one the acting peer MEANT. The belt itself is also not withdrawn (removing it resurrects the 2026-07-31 `EquipmentNotSelected` grenade/reload rejects); with L240 it simply has nothing left to do for a peer's own click. Falsified RED on three arms (`SelectionMoves` -> `true` and -> `false`; guard inlined past the decision; site reverted so the belt runs first) | premise-changed + POSITIVE CONTROL |
| L250 | a ping never paints through the game's SHARED highlight latch, the pinged actor is still marked by something, and the paint it does take is handed back: no method of `PingMarkers` calls `Highlight` on anything implementing `IHighlightable` or on `HighlightControllerComponent` (the LATCHED entry only — `SetSpecialShader` is deliberately NOT banned), `ShowTac` still reaches `TacticalActorKey.Resolve` and `Track`, and `Expire` still reaches `Repaint` | P4c P11 | principle | 2026-08-08 owner asked for the game's own full-body ability flash (the white a stamina-restore target wears) on pinged actors. THE ACTOR IS NOW PAINTED, as asked; what stays banned is the one ROUTE to it that cannot work. The route is `IHighlightable.Highlight(…, ability: true)`: its branch is per-actor (cloned materials, `HighlightControllerComponent.cs:179-181`) and skips the global `Shader.SetGlobalColor` the selection branch writes (`:166-170`), but the ENTRY IS LATCHED — `if (_isHighlighted == highlight) return false;` (`:144-153`) over ONE bool the game's own targeting already drives (`UIStateShoot.cs:1445` raises it with `ability: true`, `:1463` lowers it). So a ping on an actor the local player is aiming at is SWALLOWED, and the ping's expiry lowers the latch under the game, after which the game's own clear no-ops and the actor keeps a paint nobody removes — both silent. It could not have carried the colour either: the ability look is one scene-wide shader asset (`LightingSettingsCharacters.AbilityHighlightShader`, applied `:245-249`) with no per-call colour on that branch, so L251's green/blue is impossible there. The paint the ping DOES use is `SetSpecialShader` (`:269-293`) — a separate, UNLATCHED, per-actor slot the game itself parks a whole-body look in (`StanceStatus.cs:41,56`; `HolographicStatus.cs:30,38`), which `SelectShader` (`:177-187`, `:192-195`) lets the highlight branch win over rather than swallow, and which is driven with `BodypartHighlightShader` (`:169`), the one look the game does colour. It was banned here until the retarget of 2026-08-08 and that ban banned the fix. Because that slot is SHARED with the game, the `Expire`→`Repaint` arm is what returns it to whoever held it — without it a pinged soldier keeps the ping colour for the rest of the battle and his stance or holographic look never comes back. The premise arm re-opens the question if the game ever unshares the latch | premise-changed + POSITIVE CONTROL |
| L251 | the ping COLOUR answers who pinged, decided at the viewer: `PingMarkers.Own` is recognisably green and `Peer` recognisably blue (values read, with a channel margin), `Show` hands `ShowCore` `false` and `ShowLocal` hands it `true` (read off the IL byte in front of each call), and `OnGUI` loads both colours and reads `Live.Mine` so the off-screen arrow carries the same answer | P11 | principle | 2026-08-08 owner: each peer must see its OWN pings green and every other peer's blue — so one ping is green on one screen and blue on the other four, which is correct and not a desync. Nothing on the wire says who sent a ping (deliberately: the packet stays byte-identical across builds), so WHICH DOOR the payload came through is the entire answer, and if the two doors ever pass the same constant every ping on every screen silently becomes one colour with nothing thrown and nothing logged. L182 arm (c) stays green through both that and through the two COLOURS being the same colour. The arrow half matters most: it is what a player sees when the ping is off screen, i.e. exactly when the marker cannot answer, and it shipped in a fixed white | POSITIVE CONTROL |
| L252 | the OFF-SCREEN ARROW is big enough to hit, takes a click, takes the clicker to the target, and does not also order a soldier there: `ArrowHalfMin`/`ArrowHalfFraction` are at least twice what shipped (12f / 0.02f), `OnGUI` hit-tests with `Rect.Contains` and reaches `Focus`, `Focus` calls `CameraDirector.Hint` and builds BOTH `CameraChaseParams` (battle) and `GeoCamDirectorParams` (globe), and both `[HarmonyPatch]` gate classes read `PingMarkers.CursorOverArrow` over `TacticalView.IsCursorOverGUI` / `GeoscapeView.IsCursorOverGui` | P4c P11 | principle | 2026-08-08 owner: the arrow was ~21px at 1080p and led nowhere, so "who pinged what" was still the watcher's problem to solve. Camera recipes are the game's own — `CameraChaseParams` is what `TacticalActorViewBase.DoCameraChaseParam`:491-501 builds (the method behind a portrait click), `GeoCamDirectorParams` is verbatim `GeoscapeView.ChaseTarget`:1109-1113 — with `ChaseTransform` left NULL and `ChaseTarget` itself not called, because a chase naming a transform never self-ends (`PlanarScrollCamera`:747/:838) and `ChaseTarget` also installs the globe's sticky snap, either of which glues a watcher's camera to somebody else's soldier (the L162 defect). The fourth arm is the non-obvious one: an IMGUI rect is invisible to `EventSystem.IsPointerOverGameObject`, so without telling the game's OWN cursor gate the truth the click follows the ping AND falls through to the world — on the battlefield an order to walk the selected soldier to the screen edge (`TacticalViewState.cs:47-70` gates world clicks on exactly that method), on the globe a camera drag (`GeoscapeCamera.cs:187,268-286`). `Event.current.Use()` does not help: the game reads the mouse through Rewired. This law is the other side of L160's narrowing — a packet may not move a camera, a click MUST | premise-changed + POSITIVE CONTROL |

| L260 | a queued window whose SUBJECT has already resolved is never SERVED: `WindowOrder.DropResolvedSubjects` removes exactly the marked entry from a real `List<GeoscapeViewStateSwitchRequest>` and leaves the others in order, removes nothing when nothing is marked, reports one description per removal, drops a real `UIStateGeoModal` whose `_modalData` is a real COMPLETED `GeoMission` while KEEPING its mission-less twin, and is reached from `ReadyToDequeue`, which is the prefix on the game's one drain `GeoscapeViewSwitchQuery.ProcessQueriedStateSwitch` | P1 P11 | live bug | 2026-08-08 owner: returning from a battle, the host was met by the START-MISSION window for the mission that had just been completed, and the clients by a different set. The mod already filtered resolved subjects — but only in `RestoreDropsResolvedSubjects`, a Prefix on `GeoscapeViewSwitchQuery.RestoreData`, which asks ONCE while the queue is rebuilt. `RestoreData` does not CONSUME the list: `ProcessQueriedStateSwitch`:57-70 pops entries one at a time, later. Measured on the host, one clock: `"1 entries in the save, 1 kept — 0 dropped (subject already resolved)"` at 02:49:11.734 (multiplayer.log:3378) and the mission resolved at 02:49:12.015 (`activeMission=none`; structural destroy of `S#213.SerializationData.ActiveMission` at :12.254) — the filter asked 281 ms before the answer existed. **L117 was GREEN through all of it and could not have caught it**: it asserts the filter is a Prefix on `RestoreData`, that `HasResolved` reads `IsCompleted` and `GetMissionOutcomeState`, and that `SubjectMission` is called rather than a `ModalType` switched on — every one of those was true while the stale window was on screen. It is a law about the CALL and the SHAPE; the defect was in the MOMENT. Fix moves the question to SERVE time, generically: nothing names a `ModalType`, a window kind or a mission family — the subject is field-walked out of whatever state the request carries and the GAME's own predicate (`UIStateInitial.EnterState`:102) judges it. That reaches what the restore filter never could — `UIStateRosterDeployment` (the "start mission" squad screen, `_mission`:29, queued at `ToDeploymentState`:596) is NOT an `IGeoscapeRestorableViewState` and never appears in a save's queue at all — and it revalidates HELD one-shots on release (the `PROG_NJ0_MISS 'REPLAYED'` shape, L189). The reward windows are untouched and not by exemption: none of the four event states holds a `GeoMission` (verified in the decompile), and the outcome modal is opened by `UIStateInitial.EnterState`:112 after the queue is rebuilt, so it is not in this queue. Falsified RED on every arm in turn (pruner removes nothing -> `stale-window-is-served`; removes everything -> `live-window-is-dropped`; verdict always null -> `resolved-subject-survives-the-drain`; verdict always names -> `unknown-subject-is-dropped`; call deleted from `ReadyToDequeue`, and patch moved to `FinishCurrentStateSwitch` -> `prune-off-the-drain-seam`; `FakeSeam` given a working body -> `control-not-red`) | premise-changed + POSITIVE CONTROL |
| L261 | host and clients decide a window is dead by the same PEER-BLIND rule: no member of the staleness path (`ResolvedSubjectName`, `HasResolved`, `SubjectMission`, `DropResolvedSubjects`) reaches `NetworkEngine` or takes a host/peer parameter, `ReadyToDequeue` reaches the prune BEFORE its first `NetworkEngine` read, and exactly ONE `HasResolved` exists in the assembly | P1 P2 | live bug | 2026-08-08, same boundary, one wall clock across three local instances: host `"1 entries in the save, 1 KEPT"` (02:49:11.734), client-2 `"1 entries … 0 KEPT — 1 dropped (Mirrored kind)"` (02:49:11.584), client-3 the same (02:49:09.968). The host ended holding one window the clients did not have, and it was the start-mission window for the mission all three had just finished. Careful about what this does NOT assert: L191 pins the opposite — restore KEPT COUNTS are MEANT to differ (the host restores its own blob, a client re-carries its own copy through `EventPopup.RequeueUnanswered`), and "make the counts agree" is a regression in both directions. The divergence measured here is DOWNSTREAM of the single L260 defect: the host kept a window it should also have dropped. So the convergence property is not equal counts but a verdict with no peer in it — two peers agree about a dead window the way two calculators agree about a sum, not by exchanging one. That is simultaneously the NO-QUORUM guarantee stated as an invariant: a rule with nothing peer-shaped in reach cannot be made to wait for anyone to ACT. L260 arm (c) executes it — RailCheck has no `NetworkEngine`, no session and no roster, and the resolved window is dropped anyway. Falsified RED on every arm in turn (`NetworkEngine.Instance` read inside `ResolvedSubjectName` -> `verdict-asks-a-peer`; an engine read moved above the prune -> `prune-is-session-gated`; a second `HasResolved` added to `WindowOrder` -> `two-verdicts-can-drift`; `FakeSeam.AsksTheEngine` stopped reading the engine -> `control-not-red`) | premise-changed + POSITIVE CONTROL |
| L300 | a `LocalizedTextBind` the HOST RENDERS AS TEXT arrives as that text on the client, and one that renders as nothing is diagnosed ONCE rather than on every walk: an EXECUTED `EncodeLeaf`->`DecodeLeaf` round-trip of a literal bind `Localize()`s to the same string, a localizable bind comes back with its `LocalizationKey` intact and still localizable, and 500 encodes of a keyless bind leave exactly 1 gate entry and 0 miss-tally families | P1 P6 | live bug | 2026-08-08. One 3-instance session emitted, from a single warning inside `RailMeta.EncodeLeaf`: x307944 `GeoSiteInstaceData.Motto`, x302022 `GeoSiteInstaceData.Name`, x7896 `GeoPhoenixBase.InstanceData.LocationDescription` — ~45k suppressed lines per 30-second digest, the dominant log cost of the whole run, each line predicting a blank site label on the client. THE PREDICTION WAS ACCURATE AND THE DEFECT DID NOT EXIST: `LocalizedTextBind` has exactly two members (`LocalizationKey` + `_doNotLocalize`, decompile `Base.UI/LocalizedTextBind.cs:11/:13`), this codec already carried BOTH, and `Localize()` returns `LocalizationKey` VERBATIM whenever the key is empty (`:35-42` — the guard is `!_doNotLocalize && !IsNullOrEmpty(key)`), so no bind can render non-empty text out of an empty key and those sites were blank ON THE HOST TOO. An empty bind is in fact the game's OWN spelling of "no text": `GeoPhoenixBase.cs:1120-1122` constructs one to mean it and `:145-151`/`:159-166` refuse to overwrite a name or motto from a template whose key is empty, `GeoSite.cs:1557-1558` does the same on load, and `UIModuleSelectionInfoBox.cs:603-610` HIDES the motto row when `Localize()` comes back empty. So 610k lines reported the mirror WORKING. The warning asserted a PROXY ("the key is non-empty") that is neither necessary nor sufficient; this law asserts the OUTCOME by EXECUTING the shipped codec and asking the two binds what they RENDER — the same question the geoscape asks when it draws the label. The KEY rides and never the host's resolved string, so each peer resolves it through its own `LocalizationManager` (`Localize` passes `language = null`, `:39`) and a client on any of the 8 shipped languages reads its own translation; the `_doNotLocalize` flag rides with it so a runtime-composed literal arrives verbatim instead of as a failed lookup. Damped, NOT deleted — `WarnOnce` dedups by finished MESSAGE, so naming the member (`EncodingWhere` = reflection + concatenation) had to run on every walk just to learn it was a repeat; the gate is keyed on the cached `RailField` reference, costs one hash, and the once-per-member body is the only place allowed to reach I2. Falsified RED on every arm in turn (`w.Write(false)` for `BindNoLocalize` -> `text-does-not-survive`; `w.Write(bind.Localize())` for the key -> `host-language-shipped`; the `_keylessBindSeen` gate deleted -> `rediagnosed-every-walk` at 0 gate entries; the old per-walk `WarnOnce` shape restored -> BOTH halves, including 1 miss-tally family; `FakeSeam.DropsTheText` made lossless -> `control-not-red`) | premise-changed + POSITIVE CONTROL |


| L270 | a player ability may not write a rail-covered root on a client, whichever root it is: `Program.RailCoveredRoots` still names all four (`GeoVehicle`/`GeoSite`/`GeoHaven`/`GeoFaction`), the shared sweep still REACHES `GeoHaven.PrepareHavenMission` (which lives only inside `StealAircraftAbility`'s modal callback, so the discovery must follow an ability into its own closures), and every gesture it reaches is covered by `Program.CoveredGesture` — our prefix, or every root method it funnels into carrying one; reads (`GetAvailableStealAircraftMission`) are not gestures | P1 P2 P7 | live bug | 2026-08-08. A CLIENT pressed STEAL AN AIRCRAFT and got "busy with something else". Host: `S#79 — launch: that site has no runnable mission any more` -> `HOST mission REJECT peer=1`; client 200 ms later: `[MP][site] repaint S#79 … activeMission=StealAircraftAN_CustomMissionTypeDef`; sealed by `CRC backstop: root 'S#79' DIVERGED on peer 1 (host A3B2BD63 != client 2B848FC9)`. `StealAircraftAbility.ActivateInternal`:92 -> `GeoHaven.PrepareHavenMission` -> `Site.SetActiveMission` (GeoHaven.cs:1091), unpatched. **L42 was GREEN through all of it**: its receiver filter was the single type `GeoVehicle` while its own comment claimed the set was "DISCOVERED, never declared" — a filter naming one type IS the declaration. Widening it also exposed `GeoFaction.CreateScanner`/`CreateAncientSiteProbe` (`ActorSpawner.SpawnActor` from `ScanAbility`/`AncientSiteProbeAbility`), now gated by `FactionSpawnerGestureGate`. Falsified RED on all four arms (drop `GeoHaven` from the root set; stop following closures; delete the `HavenMissionGate` prefix — reds BOTH `PrepareHavenMission` and the `PrepareDummyMissions` wrapper; empty `FakeSeam.MintsAMission`) | premise-changed + POSITIVE CONTROL |
| L271 | on a client the infiltrate/steal confirm mints NOTHING — `site.ActiveMission` stays null until a rail apply lands the host's: `HavenMissionGate.MintsLocally` EXECUTED over its whole truth table (a client with a wired mission it does not hold is BLOCKED; `wireMission:false` stays native so the brief modal is not emptied; host/solo/apply stays native; and an already-arrived mission runs native so the game's own early-out hands back the HOST's instance), and the live `Prefix` still CONSULTS that decision | P1 P3 P13 | live bug | 2026-08-08, same session as L270. The refusal string was ambiguous by construction — `Validate` collapsed `mission == null` and `!IsRunnable` into one sentence, and `GeoMission.IsRunnable` has ZERO overrides in the shipped assembly, so it could only ever have meant null. Split into two refusals: "no ActiveMission AT ALL … the peer that sent this minted one locally" vs "exists but is NOT RUNNABLE". The gate sits at the FUNNEL, not the ability: the same `PrepareHavenMission` serves `HavenFacilityController`:110 and `HavenInteractionController`:219, so one prefix covers every haven infiltration. Falsified RED on all six arms (`MintsLocally` -> `true`; drop each of its three terms in turn; inline the decision in the prefix; make `FakeSeam.AlwaysMints` return the real answer) | premise-changed + POSITIVE CONTROL |
| L272 | a watch armed with a NULL `EventId` still opens the squad screen and is still bounded: `MissionArrivalNav.AnswerIsIn` EXECUTED both ways (an eventless watch does not wait for a record; an event-backed one still does), `Step` still CONSULTS it and still reaches `MissionHasArrived` + `GeoscapeView.LaunchMission`, and `WatchHavenMission` stamps `Time.realtimeSinceStartup` at the CLICK so `ArrivalGivenUp` can fire | P11 P13 | live bug | 2026-08-08. A haven infiltration has no `GeoscapeEvent` anywhere in it, so the watch `MissionEncounterNav`/`MissionArrivalNav` was built for could not serve it: `Watch` required an `eventId` and `Step` gated on `EventPopup.LiveRecord(id)` resolving. Armed as-is it would have waited forever on a record nothing writes and the screen would never appear — silently, this repo's dominant bug class. A null `EventId` means the answer term is THIS PEER'S OWN CLICK, already true; only that term differs, both arms still wait on the mission and both are still bounded at `ArrivalWindowSeconds`. NOT A QUORUM: every term is this peer's own record/site/view queue, and the bound drops LOUDLY (`arrival watch … GIVEN UP`). Falsified RED on all six arms (`AnswerIsIn` -> `recordResolved` and -> `true`; inline it in `Step`; drop the `MissionHasArrived` call; stop stamping the clock in `WatchHavenMission`; make `FakeSeam.AlwaysNeedsARecord` the real answer) | premise-changed + POSITIVE CONTROL |
| L273 | the haven zone crosses the 0xB8 wire as an INDEX into `haven.Zones` and the host never re-derives it by mission tag: the client seam resolves the chosen zone with `IndexOf`, `HandlePrepareHaven` reads the i16 back, and that handler reaches NO `FirstOrDefault`-shaped search and never touches `AvailableMissionsTags`; plus the game-side premise that `GeoHavenZone` still declares no id member | P2 P3 | principle | 2026-08-08, decided while fixing L270/L271. `GeoHavenZone` has no rail identity and never will — no id member (its surface is `Def`/`ZoneCount`/`Health`/`Haven`/`State`), CREATED by its haven (`BuildZone` -> `_zones.Insert`, GeoHaven.cs:410), and `IdentityResolver.HavenZoneRef` is an ALIAS path into that list. A tag lookup is NOT an equivalent recovery: `GeoHavenZoneDef.AvailableMissionsTags` is a LIST and several zones of one haven can carry the same `MissionTypeTagDef`, so the game's own `Zones.FirstOrDefault(...)` (GeoHaven.cs:1061) is a FALLBACK for callers that passed nothing — re-running it host-side can mint the mission against a zone the player never saw, and the zone is what the mission is constructed against (population, defenders, the aircraft being stolen). Index -1 = the caller passed no zone (`HavenInteractionController.MissionSelected`:219), reproduced host-side by passing NULL so the game runs its own fallback, identical to solo. Falsified RED on all five arms (drop the `IndexOf`; read an i32 instead; `FirstOrDefault` the zone host-side; point the id probe at a member that exists; empty `FakeSeam.DerivesByTag`) | premise-changed + POSITIVE CONTROL |
| L290 | a client's exploration timer can only ever finish AFTER the host's, and a client never starts one the host has not ordered: `GenericApplier.ExplorationStartInLocalEpoch` executed on all four corners — no host edge and nothing running yields NO start at all, the host's edge anchors on THIS peer's `Timing.Now` and never on the mirrored value, an existing stamp survives an unrelated order delta, and a stamp-less RUNNING handle (the join case) is adopted rather than torn down — plus the decision is reached (`ReseedExploration` calls both the anchor and `ShouldBeExploring`, `FlushOrderReseed` reads `_hostExplorationEdge`) and the epoch is RECORDED (`_localExplorationStart`) | P3 P1 P13 | live bug | 2026-08-08, 3 instances. Both clients at 02:50:22.69: `exploration NOT re-seeded for V#1@8be7e872... parked at S#79 — the mirrored start 01:06:09.4280000 + the site's def-fixed ExplorationTime ends at 05:06:09.4280000, which the clock (06:26:33.4280000) is already past — a STALE start`; 15 s later ONE client alone repainted `S#79 ... activeMission=StealAircraftAN_CustomMissionTypeDef`, sent a launch intent, was answered `HOST mission REJECT peer=1 ... that site has no runnable mission any more`, and produced the session's ONLY CRC divergence (`root 'S#79' DIVERGED on peer 1, host A3B2BD63 != client 2B848FC9 at quiescent seq 153`). **The five hours are not lag, they are two different zeros.** `StartedExplorationAt = base.Timing.Now` (GeoVehicle.cs:423) reads the PER-ACTOR clock: every `ActorComponent` gets its own `new Timing()` (ActorComponent.cs:85) and the ONLY `Timing.StartTime` writer in the game is `GeoLevelController`:571, the LEVEL clock — so `Now = StartTime + OwnNow` (Timing.cs:55) is that actor's AGE, zeroed when its `ParentTime` was assigned (:148) and rooted in that PROCESS's `Time.time` (:176). The rail mirrors the LEAF (`docs/rail-baseline.txt`:727/:753) and DELIBERATELY EXCLUDES the clock it is measured in (:33 — `per-actor clock ... client clocks tick locally, the level clock rides the TimeAnchor 'TA' root`): a mirrored number in an unmirrored epoch. The old `now < start + ExplorationTime` test was therefore never meaningful and failed BOTH ways — past the skew a client mirrors no exploration at all and would tear a running one down, short of it a client's timer can expire BEFORE the host's. **L115 was GREEN through all of it and could not have caught it**: it executes the same decider with host and client numbers drawn from ONE epoch, which is precisely the assumption that does not hold across peers. Fix takes the EDGE, not the value — exploration has ONE starter (`ExploreSiteAbility.cs:14`, sole caller of `StartExploringCurrentSite`:414, verified over the whole decompile) and a client's own click is an INTENT (`ClientSimGate.cs:341`), so the ARRIVAL of the `StartExplorationTime` delta IS the host's order, and stamping `Timing.Now` at that instant makes the client's timer start LATER by the wire delay and run the identical def-fixed duration. Not a repair of the arithmetic but a structural one: the client cannot complete first, so it cannot be the author. The `inspected` short-circuit still outranks the local clock (L115's subject, untouched) and `SiteExploredOutcomeGate` still blocks the outcome. Diagnostic kept and sharpened — it now prints the local stamp, the local clock AND the host's own leaf, and says the epochs are not comparable. Falsified RED on every arm in turn (fall back to the mirrored value -> `phantom-exploration`; anchor the edge on it -> `client-can-finish-first`; drop the stamp short-circuit -> `fill-restarted`; drop the already-exploring term -> `join-spinner-killed`; inline the anchor / inline the decider -> `decision-unreached` x2; stop touching `_localExplorationStart` -> `no-local-epoch-recorded`; stop reading `_hostExplorationEdge` -> `decision-unreached`; `FakeSeam` given a working body -> `positive-control-dead`) | premise-changed + POSITIVE CONTROL |
| L291 | no mirrored PER-ACTOR-CLOCK timestamp can decide state on a client: the facility construction clock stays unreachable there — a sweep of the game's own geoscape namespaces finds `GeoPhoenixFacility.UpdateFacility` called ONLY from `GeoPhoenixBase.BaseHourlyUpdate`, that only from `GeoPhoenixFaction.UpdateBasesHourly`, and that only from `GeoLevelController.LevelHourlyUpdateCrt`, which `ClientSimGate` still prefixes | P3 P1 | live bug | 2026-08-08, the universal-first half of L290. Exploration was never the only member of the class: `docs/rail-baseline.txt` mirrors three families of TimeUnit leaf written from an `ActorComponent.Timing`, and tracing each writer in the decompile puts them in three different places. (1) `GeoVehicle.StartExplorationTime` (:727/:753, `base.Timing.Now` GeoVehicle.cs:423) DECIDED — that is L290. (2) `GeoPhoenixFacility._nextUpdateTime` / `_prevUpdateTime` (:418/:420, `PxBase.Site.Timing.Now` GeoPhoenixFacility.cs:375-376) DECIDES: `UpdateFacility`:366 flips a facility to Functioning on `NextUpdateTime <= PxBase.Site.Timing.Now`, which on a client would complete a construction the host never built — minted state the diff can never correct, since the diff is host-now vs host-before. It is inert TODAY for exactly one reason, the hourly chain above, and NOTHING anywhere said so: a second caller of any link, or a narrowing of `ClientSimGate` off `LevelHourlyUpdateCrt`, arms it silently. That dependency is what this law writes down. (3) `GeoHarvestingSite.StartedHarvestingAt` (:775, `_timing.Now` where `_timing = actor.Timing`, GeoHarvestingSite.cs:111/:136) decides NOTHING — its only consumers are `SetProgression`:114/:168, and the completion is a local updateable driven by the mirrored `HarvestingForce`; recorded in the law's header rather than asserted, because a law over a field that reaches no decision is decorative. (`GeoSite.ExpiringTimerAt` reads like a fourth and is not one: it is assigned from `GeoEventTimer.EndAt`, GeoscapeEventSystem.cs:535, a LEVEL-clock value, and the level clock DOES ride the rail.) Asserted as REACHABILITY over the game's own IL, not as a call we make. Falsified RED on every arm in turn (retarget `ClientSimGate` off `LevelHourlyUpdateCrt` -> `hourly-gate-moved`, a real defect edit; break the allowed-caller set -> `facility-clock-ungated`; break the namespace filter -> `scan-empty`). The caller-set arm cannot be falsified by adding a real second caller — the game assembly is not editable — so its RED was produced by breaking the expected set, stated here rather than left implied | premise-changed |
| L310 | after this peer's UI comes off an actor mid-sweep, NO `ArgumentNullException`/`NullReferenceException` escapes the move-overlay coroutine and it is never left reading a null source: `TacticalCommandSync.MoveOverlayMustNotSeeNull` is EXECUTED on all four corners and a Harmony POSTFIX on the game's own `MoveAbilitySceneViewElement.ValidMoves` getter hands that read an EMPTY, non-null list on exactly the frames L168 withholds the sweep -- while a null the ENGINE produced on its own, and every real sweep, are left byte for byte | P1 P11 P4c | live bug | 2026-08-08, 3 instances. ONE client (`D:/PP-Instance3/Player.log` 9209/10871/11567/13217, 02:41:56 / 02:45:15 / 02:47:20 / 02:48:41): `ArgumentNullException: Value cannot be null. Parameter name: source` out of `System.Linq.Enumerable.Where` inside `MoveAbilitySceneViewElement+<UpdateMoveAreas>d__36.MoveNext`, each followed IN THE SAME FRAME by Unity's `CHECK/REPORT PREVIOUS ERROR!!! Broken coroutine call chain` -- which ABORTS the chain, so every coroutine downstream of it silently stops for the rest of the battle. The host was clean; only the peer whose UI was released was hit. **L168's empty array was the CAUSE, not a half-fix.** `MoveAbility`:26 declares `HasValidTargets => GetTargetsData().Any()` and `TacticalAbility`:465-468 turns `!HasValidTargets` into `AbilityDisabledState.NoValidTarget`, so the withheld EMPTY sweep is precisely what reports the ability NOT ENABLED, and `MoveAbilitySceneViewElement.ValidMoves`:69-79 then answers `null`. `UpdateMoveAreas` re-reads that property AFTER its yields (:237, :243, :253, :259) and guards it exactly ONCE, at :223, before the first one -- so a draw that was legal when it started and withheld one frame later hands `Where` a null source. The log shows both halves in order: frame 37934 `released this peer's UI from Soldier_9 (a mirrored Move_AbilityDef)` then `Ability Move_AbilityDef ... activated by Soldier_9`; frame 37935 `move-range sweep WITHHELD for Soldier_9` then the throw. **FED, NOT STOPPED, and the reason is mechanical:** `ValidMoves`:73 evaluates `IsEnabled()` -- and so reaches the withhold -- BEFORE it returns, two instructions from the caller that dereferences it, so any cancellation the seam could reach for lands after the exception. `SceneViewElement.StopDrawing` (`MoveAbilitySceneViewElement.cs`:139-154, what `UIStateCharacterSelected.ExitState`:584 calls) cannot help either: it already ran inside `ResetViewState` on frame 37934 -- `StateStack.Clear`:106-118 calls `Exit` SYNCHRONOUSLY -- and the coroutine that threw was a LATER one, restarted by the game's own redraw path. Feeding an EMPTY list is the whole fix and needs no cancellation machinery: :240 `flag` goes false, `UpdateMoveArea`:268-271 yield-breaks on the empty list, and the coroutine ends normally having drawn nothing over the `ClearGroundMarkers` it already did at :227 -- which IS the blank overlay L168 promises, on the same frame, with no re-enter. Every LATER restart (`UIStateCharacterSelected`:1165 `StartDrawing` -> `DrawTargetMarkers`, and :214/:255/:757) re-enters :223 where `HasValidMoves` is now false and yield-breaks BEFORE its first yield. **Sited on the PROPERTY, not on the release**, which is what makes it cover every path that takes this peer's UI off an actor mid-sweep -- `ApplyActivate`'s release for ANY mirrored ability (not the Move that was reported), the FORCED settle's release, and the standing L168 case where nothing released anything and the player merely re-selected. Unaffected by `f9ecd0e`: that moved WHEN `ReleaseLocalUiHolding` runs, and this seam does not read the release at all. Falsified RED on every arm in turn (decision always-false -> `overlay-still-sees-null`; always-true -> `engine-null-overwritten` + `real-sweep-replaced`; `__result` by value -> `overlay-seam-cannot-answer`; postfix stops consulting the decision -> `overlay-feed-is-unconditional`; `Empty` given a member -> `fed-sweep-is-not-empty`; the patched property misspelled -> `overlay-patch-cannot-bind`; the three axes inlined back into L168's prefix -> `gate-and-feed-disagree`; `FakeSeam` corrected -> `control-not-red`; the premise detector broken -> `premise-changed`). L168 arm (d) AMENDED in the same change: it demanded the decision be called from the prefix ITSELF -- a shape we control -- and turned red on a fix whose whole point is that the withhold and the feed ask ONE question through a shared `SweepIsWithheldFor`; it now follows ONE hop | premise-changed + POSITIVE CONTROL |
| L311 | two destructibles standing in the same place still have two addresses, and an address that cannot be checked is not used: `TacticalDestruction.AddressTags` is EXECUTED and must give n co-located objects n DISTINCT addresses, must produce NOTHING in common between the same cell holding n and holding m≠n, must not let one cell's addresses collide with a neighbouring cell's -- and `OnEnvironmentDamage` must ship the tag the INDEX minted, never a bare cell of its own | P1 P4c | live bug | 2026-08-08, 3 instances, IDENTICAL numbers on both clients: `358 destructible(s) stand within a tenth of a unit of another, so the position address cannot tell them apart. One of each pair is unreachable by that address` (`TacticalDestruction.cs`:203) alongside `9 destructible guid collision(s)` and `indexed 1251 destructible(s) of 1269`. **The three were never three holes.** 1269 walked − (9 collided guids × 2 objects) = 1251, so the "18 never indexed" IS the guid collision, exactly; the index log now states that arithmetic itself so no future sweep chases it again. The REAL hole is the intersection: a collided guid is dropped on every peer by design (`SceneObjectIdsComponent.MergeWith`:29-34 replaces it with a fresh RANDOM id, so it is not an identity), and the old `_byPos` let the FIRST object the walk reached keep the whole cell -- an object with neither took the host's damage nowhere, silently. **A finer rounding could not have fixed it.** A destructible's `transform` is its PREFAB PIVOT and the game never locates one by it: `Destructable.Init`:647 reads `MeshRenderer.bounds`, :721 keeps `_gridOrigin = bounds.min`, :712-716 places one receiver GameObject per tile from `GridToWorld(bounds.min…)` -- a kit-built wall set sharing one pivot puts every panel of a building at a BIT-IDENTICAL world point, which no number of decimal places separates. Nor could the bounds be the address instead: `CheckAndDisableSelf`:254-256 DESTROYS the MeshRenderer and Collider of a fully broken object, so a bounds-derived address evaporates exactly when the object is still present and still being addressed. **So the discriminator is the walk this arc already stakes everything on**: the address is `cell#i/n` -- rounded cell, index among the n found there by `GetComponentsInChildrenStable` (`UnityUtil`:69-74, strict depth-first: own components, then children in sibling order), and n itself. The hierarchy cannot shorten mid-mission -- `CheckAndDisableSelf`:261 only `SetActive(false)`, `Breakable.DisableSelf`:374 only clears `enabled`, and an inactive child is still walked -- so a tag minted at battle start stays true for the whole battle. **And n RIDING IN THE KEY is the property a bare ordinal never had**: a peer that found a different number in that cell matches nothing and says so, instead of confidently naming the wrong wall; the failure line now reports its own occupant count next to the host's. Removes all 358 by construction (every occupant of every cell is now addressed, not just the first). Falsified RED on each arm in turn (drop the `#i` -> `co-located-share-one-address` ×3; drop the `/n` -> `address-not-self-checking` ×2; capture back to `PosCell` -> `capture-derives-its-own-address`), GREEN restored after each. L69's second-address arm renamed `PosTag`->`PosCell` in the same change and still asserts the index reaches it | premise-changed |
| L320 | a client reaches AI evaluation through NO door and the host still reaches it through all of them: `ClientAiEvaluationSeamGate.Prefix` is EXECUTED against a real `NetworkEngine` shaped three ways — a live session in which this peer is not the host must be REFUSED, the host must be LET THROUGH, and no session at all must be untouched — over a class the CLR itself closes (`AIEvaluationAbility.ExecuteAIEvaluation` is `private` and referenced by `Activate` alone), with the override relationship that makes every base-slot caller land in the patched body asserted beside it | P5 P3 P4a | incident | 2026-08-08 `69d75d2`. `ClientAiEvaluationGate` covered ONE caller — `TacticalLevelController.ExecuteAIEvaluationAbilities`:1257, reached as `ability.Execute(null)`. `TacticalAbility.Execute`:1158-1160 is a two-liner over `Activate(parameter)` and `AIEvaluationStatus.OnApply`:17 calls `Activate()` DIRECTLY under `ExecuteActionsOnApply` — no coroutine, no turn machinery, invisible to that gate — so a status merely MIRRORED onto a client ran `ExecuteAIEvaluation` -> `TacticalFaction.EvaluateAiActionsAsync` -> `AIFaction.EvaluateActionsAsync` and the client decided for itself. **A gate per known call path is how the hole opened**, so the subject here is the outcome on both sides of the seam, not the presence of a gate. MEASURED WHILE WRITING THE LAW AND NOT WHAT THE FIX ASSUMED: the shipped IL of `OnApply` emits `callvirt Ability::Activate(object)` — the ROOT virtual slot, not the derived method — and so do `Execute` and `ExecuteAndWait`; the patch on `AIEvaluationAbility.Activate` is reached from all three for exactly one reason, that it OVERRIDES that slot. Stop overriding and every door bypasses the gate while the gate itself still binds, which no `L125`-style binding law can see. Falsified RED on every arm in turn (gate reverted to `return true` -> `client-evaluates`; `|| engine.IsHost` dropped -> `host-refused`; the `engine == null` term dropped -> `solo-intercepted`; `FakeSeam` given the real gate body -> `control-not-red`). The two structural arms cannot be falsified by editing the game — the assembly is not writable — so their RED was produced by breaking the law's own expected set (`evaluation-has-a-second-door`, `status-door-misses-the-gate`), stated here rather than left implied | premise-changed + POSITIVE CONTROL |
| L321 | an order the engine merely ENQUEUED behind the idle loop is not reported as stalled, and one that leaves the channel without executing is named the frame it happens: `TacticalCommandSync.MirrorIsWaitingInLine` is EXECUTED on all four corners (only not-executing AND still-enqueued is a wait), `ApplyActivate` reaches NO `Debug.LogError`/`LogWarning` after the activation while still reading `TacticalAbility.IsEnqueued`:269 and consulting the decision, and `TickQueuedMirrors` carries TWO separate verdicts with the drop test reached BEFORE the first report | P1 P5 | incident | 2026-08-08 `8bc2f99`. The watchdog fired on 8 of 8 mirrors in one battle and named `IdleAbility` as the blocker every time, sending an RCA after the NORMAL path: `ShootAbility.Activate`:167 takes `EnqueueAction` on every player-turn click (host and client alike), `EnqueueAction`:947-951 appends behind the resting soldier's never-ending `IdleAction`:276-292, and the same call runs `TacticalActor.OnAbilityEnqueued`:1361-1365 = `IdleAbility.RequestStop()`, so the loop ends on its next resumption and `ActionComponent.CompletedAction`:112-119 starts ours — ONE FRAME, which reading `ExecutingAbilities` alone cannot tell from a stall. **Two opposite failures, and the law holds both ends**: an ability whose `Activate` works synchronously takes no action at all, could never start, hit the ceiling every time and was announced as never played when it had been; while an order really can be eaten — `ActionComponent.PlayActionAfterCurrent`:80-91 cancels every entry past the FIRST whenever a later solo enqueue arrives on the same actor, so a second order on that soldier deletes the one already in line. A watchdog quietened by watching less would lose exactly that case. Not replayed — `L83`; the settle remains the repair. Falsified RED on every arm in turn (decision -> `!executing`, the defect verbatim -> `took-no-action-watched`; -> `enqueued` -> `playing-watched`; the old MIRROR-QUEUED line put back after `Activate` -> `handoff-reported-as-a-stall`; the decision inlined -> `decision-unreached`; `IsEnqueued` no longer fed to it -> `decision-unfed`; the drop branch removed -> `drop-unnamed`; a report reached before the drop test -> `drop-waits-for-the-ceiling`; the control given the real rule -> `control-not-red`) | premise-changed + POSITIVE CONTROL |

- inline (private method in `Program.cs`, no file): L1–L25, L27–L76b, L81, L82, L83

- files (`L<n>_<Name>.cs`): L26, L77, L79, L80, L84, L85, L86, L91–L165, L168, L169, L171, L172, L173, L174, L178, L179, L183, L184, L185, L186, L193, L194–L199, L210, L220, L240–L243, L260, L261, L300, L310, L311, L320, L321

## Rows vs registrations — why 155 rows and 135 registered laws

The two numbers count different things and always will. `tools/law-count.txt` counts REGISTRATIONS
(one `laws.AddRange(...)` line each); this table counts NUMBERED LAWS. The mapping is many-to-many
on the inline side. Nothing is missing and no row is invented — verified 2026-08-07.

- **files: 77 rows ↔ 77 registrations, exactly 1:1.** `L26, L77, L79, L80, L84, L91–L162` — one
  `L<n>_<Name>.cs`, one `AddRange`, one row.
- **inline: 79 rows ↔ 60 registrations.** 60 inline methods emit 78 distinct ids; the catalogue
  splits one of them in two, giving 79 rows.
  - ONE method carrying MANY ids is the whole surplus. `RoundTrip()` alone emits twelve —
    L4 L7 L8 L10 L12 L13 L14 L17 L18 L19 L20 L24. `StructuralDescendLaw` emits L29 + L30.
  - MANY methods sharing ONE id happens too, and the catalogue names the arms itself:
    L76a/L76b = `TacticalFunnelLaw` + `TacticalPayloadUseLaw` (both emit the bare string `"L76"` —
    the `a`/`b` suffix exists only here, never in the code); L69 = `InventoryAndDestructionLaw` +
    `…Part2` + `ValidateProbes`; L15 = `OwnerBackRefCodecLaw` + `OwnerBackRefLaw`;
    L28 = `RootOwnershipLaw` + `SubEntityRefArm`; L49 = `ModalCoverageLaw` + `OneProducerPerWindow`.
- **1 row is not executable at all:** L78, prose-only, no arm ever. See below.
- Arithmetic: 70 file rows + 79 inline rows + L78 = 150 rows. 70 files + 60 inline = 130
  registrations. The 20-row difference is 19 (inline ids beyond their methods) + 1 (L78).
- The `147 vs 127` figure in the earlier handoff was the same accounting one snapshot earlier
  (`files=67`, three fewer rows). The ratio, not the totals, is the fact.
- **Consequence for review:** an inline law's id is NOT a registration. Deleting `RoundTrip()`
  silently retires twelve numbered laws while `inline=` drops by one. `tools/law-integrity.ps1`
  cannot see that. Prefer a new `L<n>_<Name>.cs` file for anything new — it restores 1:1.

## Unassigned / retired numbers

- L78 — prose-only, no executable arm ever. Cited `src/Tactical/TacticalCommandSync.cs:2371/:2434/:3044`, and by L104 + L127. L104's header states it.
- L26 (old) — record-derived event backlog, deleted 2026-07-30 with the engine it asserted; number re-used by `L26_PauseAndOneShot.cs`.
- L85, L86 — ISSUED 2026-08-07 (`L85_RestartedHostStreamIsApplied.cs`, `L86_AnnouncedBoundaryHoldsItsAnnouncer.cs`). Taken from the never-issued block on purpose: five agents were minting laws in one tree that day and three successive max+1 picks collided.
- L87–L90 — never issued. No file, no method, no citation anywhere in the repo.
- inline laws have no `L<n>_<Name>.cs`; grep `Program.cs` for the id string.

## Attention

- rows in table: 150. numbers issued: 155 (L85–L90 never issued; old L26 retired). Registrations: 130 — see "Rows vs registrations".
- origin: incident 96 | principle 53 | unclear 2 (L18, L78).
- guard: premise-changed only 44 | POSITIVE CONTROL only 6 | both 9 | neither 92.
- guard = POSITIVE CONTROL only: L47 L49 L53 L54 L55 L56 — all inline.
- guard = both: L77 L81 L84 L91 L92 L136 L153 L154 L155 — only L81 inline.
- guard = neither (92): L1–L25, L27–L46, L48, L50, L51, L52, L57–L76b, L78, L80, L82, L83, L93, L96, L98, L100, L101, L103–L109, L112, L113, L114, L124, L125, L126.
- guard tokens are matched CASE-INSENSITIVELY (`tools/law-integrity.ps1:77` uses PowerShell `-notmatch`), so `Positive control:` in a header counts. This column follows that same rule — not the literal casing.
- CAVEAT on `neither`: many carry an equivalent arm under a different name — `NON-VACUITY:` (L50 L52 L61), "the law's own PREMISE, executed" (L59), "anti-vacuity" (L55), "non-vacuity, both halves" (L48), "non-vacuous in both directions" (L9 L22 L49). `neither` = the two canonical tokens are absent, NOT "the law can silently pass while checking nothing".
- laws with neither token AND no differently-named equivalent visible in the header: the early codec block L1–L20.
- P orphans: none. No law maps to `P?`.
- P with zero laws: P9 (journal).
- ~~P with no law scoped to it alone: P4c.~~ **CLOSED 2026-08-07 by `L158_PresentationSeamAltersNothing`** — the three clauses below, over the seam set below, plus a fourth arm containing the new RTT reading (`PingTable`) at birth rather than retrofitting it. Two clauses landed narrower than this entry assumed and the law's header says so: clause (b) can only see DIRECT IL writes, because this seam set drives the native model almost entirely through `MethodInfo.Invoke` / `FieldInfo.SetValue`; clause (c) is asserted at the CONTAINMENT POINTS (`UiEventMap.Fire`, `OpenUiRepaint.Repaint`, `ResearchSync.PresentFromMirror`) plus "no uncontained caller of `UiNativeRepaint.TryRepaint`", because the helpers that actually invoke native code deliberately carry no catch of their own — demanding one per leaf would have been a style rule, not P4c. The original argument, kept because it is what the law is for:
  - **DECIDED 2026-08-07: this is a REAL coverage hole, not a principle that is only jointly checkable.**
  - The two are opposite directions, not two halves of one test. P11 asserts the repaint HAPPENS (reach); P4c asserts the seam CHANGES NOTHING (harmlessness). Every P11 law in this table is a reach-law, and a presentation seam that reaches the screen AND writes model state passes all forty of them.
  - P4c is cited by NOTHING executable. `grep -rn "P4c" tools/RailCheck src` = zero hits; the only mentions in the repo are `ARCHITECTURE.md:313` and this file. L126 ("a transpiler substitutes a computation, never writes one down") is the nearest shape and it is scoped P6 P3 and covers transpilers only, not the repaint/present seams.
  - All three of P4c's clauses are statically assertable over a named seam set (`UiEventMap` arms, `UiNativeRepaint.Table` entries, `OpenUiRepaint`, `ResearchSync.PresentFromMirror`): (a) no prefix in the set returns false / suppresses native — never BLOCK; (b) no member of the set writes a `[SerializeMember]` leaf — never WRITE; (c) every entry is exception-contained — never THROW into game code. Clause (c) is already an unasserted written promise at `ARCHITECTURE.md:192` ("a registered screen that throws keeps the screen + logs once").
  - The hole is about to be loaded, which is why it is worth closing now rather than noting again: HANDOFF §5c option (C) moves death presentation onto the mirror's own local playback, and §5e classes both new features (pings, player panel) as presentation. Both are P4c by construction and neither has a law. (The ping half arrived the same day and is arm (d) of L158; the panel arrived the same day too and DID inherit the seam set — `PlayerPanel` is in it, and `PlayerPanel.Sync` is a containment point. What the seam set could not carry is whether the panel is fed at all, which is L159. The ping MARKERS — §5e's other feature, and a different thing from the RTT ping — arrived 2026-08-07 too: `PingMarkers` is in the seam set and `PingMarkers.Show` is a containment point, and what the seam set could not carry there is that an arriving ping moves no camera, enters no view state and changes no selection, which is L160.)
- P13 is PROPOSED: not in `ARCHITECTURE.md`; stated only inside the corpus as a developer mandate quoted by L84 + L91; 18 laws defend it. Recommend writing it into `ARCHITECTURE.md` as a numbered principle.
- P12, P14, P15, P16 asserted by `ARCHITECTURE.md` but never numbered there → no source cites them by id today.
- `law 1` carries ≥3 readings in the corpus: (a) the two primitives Intent+Delta — `src/Rail/IntentRail.cs:11`; (b) join is a save transfer, not a delta — `src/Rail/DiffEngine.cs:33`, `ARCHITECTURE.md` P1 line; (c) never a silent swallow — `src/Rail/RailMeta.cs:1704/:1742`, `src/Rail/GenericApplier.cs:408`, `src/Rail/OpenUiRepaint.cs:331`. P1 states all three; not resolved to one.
- `law 5` and `law 10` are never defined in `ARCHITECTURE.md` — used only from `src/` (34 and 26 citations). P5 and P10 reconstructed from usage.
- HARNESS laws written in the principle spelling in `src/` prose (unfixed, outside this catalog's edit scope): `law 12` → L12 (`src/Rail/TradeSync.cs:32`, `src/Tactical/TacticalInventorySync.cs:762`); `law 19` → L19 (`src/Tactical/TacticalCommandSync.cs:1220`, `src/Tactical/TacticalInventorySync.cs:703/:902`); `law 58` → L58 (`src/Rail/DiffEngine.cs:347/:1077`, `src/Rail/RailMeta.cs:1967`); `law 91` → L91 (`src/Rail/GeoModalMirror.cs:34/:549`, `src/Rail/GeoWindowCoverage.cs:187`, + L99/L107/L109/L110/L111 headers).
- `ARCHITECTURE.md` rename RE-VERIFIED 2026-08-07, line by line. No numbered "law N" survives anywhere in the file; no principle was renumbered. Every `L<n>` in it is a harness law and reads correctly: L17 (:83, duplicate root key), L14 (:140, twin coercions), L11 (:273, LocalizedTextBind static belt), L1–L13 + L20 (:376-424, Verification section). No conflation with old "law 11" — `:273` says "no `LocalizedTextBind` rides covered", which is L11's subject verbatim, not P11's repaint.
- AMBIGUITIES IN `ARCHITECTURE.md`, named rather than silently resolved:
  - The file legitimately uses BOTH numbering spaces over the same digits: P1–P11 as principles and L1–L14/L17/L20 as harness laws, disambiguated only by the `P`/`L` letter and the one-line note at `:9`. That collision is structural and permanent; 11 is merely the loudest case (`P11` repaint vs `L11` text-bind belt). A bare number in any future edit is unresolvable — always write the letter.
  - `:19` "**Implementation of the law:**" — unnumbered and genuinely ambiguous. By context (canonical byte-identical deltas + generalized field enumeration) it reads as P1 or P6, but the original text names neither. Left as-is; not renamed, because picking one would be an invention.
  - `:254`/`:256`/`:260`/`:271` ("Walk-time ownership law", "No static signal carries the law", "law fails OPEN") and `:279` ("Anchor-not-Now law") use "law" as ordinary prose for an unnumbered rule. Not renaming errors.
  - P5, P10, P12, P13, P14, P15, P16 appear NOWHERE in `ARCHITECTURE.md` by number — consistent with the notes below, but it means seven of sixteen principles have no prose source there.
- laws that exist because an earlier law was GREEN through the bug it named: L102 vs L92 | L130 vs L129 | L135 vs L49+L117 | L144 vs L141 | L134 vs L124 | L138 vs L36 | L96 vs L81 | L106+L107 vs L49 | L109 vs L48 | L137 vs L131 | L162 vs L75 | L190 vs L73.
